# UI 디자인 리뷰 — 무인 루프 (사이클 1~60)

## ★★★★★ 라운드5: 탭 UI **모바일 프레임** (사이클 51~60, **모바일 우선 · 데스크톱 회귀 0**)
- **관점:** 라운드4(탭 데스크톱 97)·라운드3(모바일 96) 이후, 탭 4뷰 UI를 **모바일 기준 프레임**으로 재심사. 320·360·390·414·768·844(가로) 폭에서 탭 네비 오버플로·터치타깃·sticky 예산·개요 1열 스택·필터칩 행을 실측. 데스크톱(1280/1440) 회귀 0 동시 보증.
- **모바일 종합 점수 추이(51→60):** 91 · 92 · 93 · 94 · 95 · 95 · 95 · 95 · 96 · 96 → **최종 96/100**
- **제거한 실결함 6건:** ① 탭바 음수마진 가로 오버플로(320px 문서 +20px, *치명*) ② 활성 탭 53px 클리핑(scrollIntoView 가로 보정) ③ 탭바 스크롤 발견성(끝 페이드) ④ 가로모드 sticky 예산 31%→24%(헤더 컴팩트, 라운드3 보류 해소) ⑤ 개요 미니리스트 제목 4줄 폭주(2줄 클램프) ⑥ 필터칩 터치타깃 35→44px. (상세는 하단 사이클 51~60)
- **데스크톱 회귀 0:** 매 사이클 1280/1440/1920 동시 계측 — 헤더75·subtitle·2열·3열·도킹0·칩35 불변. `pytest 92 passed`·계약·탭기능 불변.

---

## ★★★★ 라운드4: 탭 UI 웹 (사이클 41~50, **데스크톱 우선 · 새 탭 4뷰**)
- **관점:** 단일 스크롤 → ARIA tablist 4뷰(개요/중요뉴스/전체뉴스/카테고리별) 재구성 직후. 1280/1440/1920px 데스크톱 폭에서 탭 네비·개요 정보위계·sticky 스택을 실측 평가. 모바일(390/640) 회귀 0 점검 병행.
- **데스크톱 종합 점수 추이(41→50):** 93 · 94 · 95 · 95 · 96 · 96 · 97 · 97 · 97 · 97 → **최종 97/100**
- **제거한 실결함 8건:** ① sticky 탭바 14px 겹침(top 61→75) ② 개요그리드 빈 3번째 컬럼(→명시 2열) ③ 필터칩 활성 무색(→카테고리 색 보더) ④ 탭전환 스크롤 미복원 ⑤ 개요 마지막섹션 리듬 60→40px ⑥ 스탯 조용한 카테고리 은닉(→0건 노출) ⑦ 필터 빈상태 오도 카피 ⑧ 개요 수집버튼 중복 제거. + 모바일 탭바 도킹(53→65). (상세는 하단 사이클 41~50)

---

## ★★★ 라운드3 요약 (사이클 31~40, **모바일 우선** 라운드)
- **관점 전환:** 라운드1·2는 데스크톱 우선(모바일=`max-width:640px` 단일 분기 곁다리). 라운드3는 **모바일을 기준 프레임**으로 삼아 320·360·390·414·768·844(가로) 폭에서 실측 기반 평가.
- **모바일 종합 점수 추이(31→40):** 88 · 89 · 90 · 91 · 92 · 93 · 94 · 95 · 96 · 96 → **최종 96/100** (상세는 하단 라운드3 최종 요약).
- **제거한 모바일 실결함 7건:** 헤더 뷰포트과점(125→65px)·탭타깃간격(6→8px)·태블릿 awkward 1열(minmax320→288)·safe-area 미대응·유체타이포 하드점프(clamp)·섹션액션 오버플로·이력 모바일 스택. + 설정칩 가로줄바꿈·앵커 scroll-margin.
- **데스크톱 회귀 0:** 매 사이클 1440/1040px 동시 계측 — 헤더 75px·3열 그리드·subtitle 노출·제목22px·오버플로 0 불변. 7폭+가로모드 전수 오버플로 0. `pytest 85 passed`·계약 11필드 불변.

---

# UI 디자인 리뷰 — 무인 루프 (사이클 1~30)

## ★★ 라운드2 요약 (사이클 11~30 완주, AAA·터치·상태표면 상향 기준)
- **출발점:** 라운드1 종합 98/100 (전 텍스트 AA 통과). 라운드2는 기준을 AAA(7.0)·비텍스트 대비(3.0)·터치타깃(44px)·상태표면 완성도로 상향.
- **점수 추이(11→30):** 98 · 98→99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 · 99 → **최종 99/100**
- **제거한 실결함 9건:** ① 모바일 터치타깃 28/26px→44px(WCAG 2.5.5) ② primary 버튼 포커스링 1.82→2색링(1.4.11) ③ reduced-motion 미적용 스피너/호버(2.3.3) ④ 수집/발송 status aria-atomic 부재 ⑤ 에러배너 role=alert 부재(4.1.3) ⑥ 피드백 토글 aria-pressed 부재(4.1.2·1.4.1) ⑦ skip-link 부재(2.4.1) ⑧ 영문 제목 lang 미표기(3.1.2) ⑨ 빈상태 주/보조 위계 소실.
- **AAA 상향:** `--text-dim` #8b949e→#a3acb8 — reason/meta/label/fb/h-channel 일괄 5.62→7.54(surface), 전 페이지 AAA 비율 65%(187/287).
- **상태표면 완성:** 스켈레톤 3종(알림/뉴스/이력) + aria-busy + reduced-motion off + CLS 저감(134→178px). 로딩/빈/에러 3상태 일관.
- **마이크로 폴리시:** 카드 gap 10→12px(4px 그리드·스켈레톤 동기), section-badge tabular-nums, 외부링크 "새 탭" 고지, 이력 list/listitem 시맨틱.
- **검증:** 전 텍스트 AA 미달 0(287요소)·AAA 65% · `pytest 85 passed` · 계약(`{items,total}`·11필드) 불변 · 회귀 0 · src/web/만 수정 · 360/768/1440 오버플로 0.
- **정말로 남은 한계:** ① 점수/카테고리 시맨틱 색(AA-only 100요소)은 의도된 색상 정체성이라 AAA 미추구(색 외 단서 병행으로 1.4.1 충족) ② lang 추정은 클라 휴리스틱(데이터에 언어필드 없음) ③ 스켈레톤 카드와 실카드 높이 완전 일치는 가변 콘텐츠상 불가(178 vs 206~234, 잔여 시프트 소).

---

# UI 디자인 리뷰 — 10사이클 무인 루프 (라운드1, 완료)

대상: `src/web/index.html` · 라이브: news-demo (port 8000)
루브릭: 정보위계25 · 간격15 · 컬러15 · 타이포15 · 일관성10 · 접근성10 · 반응형10 (총 100)
원칙: 계약 필드 불변(`{items,total}`·필드명), 프레젠테이션만 수정, 회귀 금지.
검증: computed style/대비/DOM eval (스크린샷 타임아웃 환경 — 폴백 사용).

> 이 루프는 직전 세션(`design_review_log.md`, 95 도달)을 베이스라인으로 이어받아 잔여 결함을 정밀 계측으로 추가 개선한다.

## ★ 최종 요약 (10사이클 완주)
- **점수 추이(1→10):** 95 · 96 · 96 · 96 · 97 · 97 · 97 · 97 · 98 · 98 → **최종 98/100**
- **제거한 실결함 5건:** fb-label 대비(2.31→5.62) · empty-hint 대비(2.53→6.15) · score-null 대비(1.96→4.77) · **primary 버튼 대비(2.84→5.17)** · 알림 카운트 배지 컬러 시맨틱(red→accent).
- **부수 정제:** score-low 토큰화, score/threshold tabular-nums, 이력 시각 `<time datetime>`, 모바일 section-header wrap.
- **검증:** 전체 텍스트 AA 미달 0(496요소) · `pytest 82 passed` · 계약(`{items,total}`·9필드) 불변 · 회귀 0 · src/web/만 수정.
- **남은 한계:** 절대 점수 척도상 알림 저점수 배지 grey(의도) · btn-sm 28px 터치타깃(모바일 1차 전환 시 검토) · `--text-dimmer`는 장식 dot에만 잔존.

## 점수 추이
| 사이클 | 종합 | 핵심 변경 |
|--------|------|-----------|
| 1 | 95 | fb-label 대비 2.31→5.62 (AA 통과) |
| 2 | 96 | 알림 카운트 배지 red→accent (점수 red 시그널 분리) |
| 3 | 96 | 모바일 section-header flex-wrap (타이틀/액션 충돌 방지) |
| 4 | 96 | score-low 색 리터럴 #8b949e → --text-dim 토큰화 |
| 5 | 97 | empty-hint 대비 2.53→6.15 (빈 상태 안내문 AA 통과) |
| 6 | 97 | 점수배지·임계값 tabular-nums (숫자 데이터 정렬) · pytest 82 green |
| 7 | 97 | 발송이력 시각 시맨틱 `<time datetime>` 화 + dead var 제거 |
| 8 | 97 | score-null 배지 대비 1.96→4.77 (널 점수 상태 AA 통과) |
| 9 | 98 | primary 버튼 흰글자 대비 2.84→5.17 (--accent-strong 도입) |
| 10 | 98 | 최종 회귀 스윕 — 전체 496요소 텍스트 대비 AA 미달 0건 |

**추이: 95 → 96 → 96 → 96 → 97 → 97 → 97 → 97 → 98 → 98**

**최종 종합: 98 / 100**

### 핵심 개선 (이번 10사이클)
- **접근성(가장 큰 임팩트)**: fb-label(2.31→5.62), empty-hint(2.53→6.15), score-null(1.96→4.77), **primary CTA 버튼(2.84→5.17, --accent-strong 신설)** — 전 텍스트 AA 통과(496요소 0 미달).
- **컬러 시맨틱**: 알림 카운트 배지 red→accent(점수 red와 분리), accent를 텍스트/보더용·버튼배경용으로 역할 분리.
- **일관성/타이포**: score-low 리터럴 토큰화, 점수·임계값 tabular-nums, 이력 시각 `<time datetime>` 시맨틱화 + dead var 제거.
- **반응형**: 모바일 section-header flex-wrap로 타이틀/액션 충돌 구조적 예방.

### 검증
- `pytest 82 passed` (사이클 6·7·9·10 반복 확인) — 프론트 변경이 백엔드 무영향.
- 계약 불변 확인: `/api/alerts`·`/api/news`·`/api/history` 모두 `{items,total}`, 9개 필드(importance_score…sent) 그대로.
- 데스크톱 2열 / 모바일 1열, 오버플로 0, 포커스 링 가시.
- src/web/index.html 프레젠테이션만 수정, 회귀 0.

### 남은 한계 (다음 기회)
- 점수 색 척도가 절대(8+/5+/<5)라 알림 섹션의 저점수(4.8) 배지가 grey — 의도된 전역 시맨틱이나, 알림 컨텍스트 전용 강조를 원하면 별도 토큰 검토 여지.
- `--text-dimmer`는 이제 `.card-meta .dot`(장식, aria-hidden)에만 잔존 — 정당.
- 터치 타깃: btn-sm 높이 28px(<44px) — 데스크톱 데모 우선이라 보류, 모바일 1차면 상향 검토 가능.

---

## 사이클 10 — 종합 98 · 수렴 완료

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 알림(블루 카운트+색보더+발송배지) vs 전체(평면) 위계 확립, 스크린샷 확인. |
| 간격(15) | 14 | 그리드·divider 대칭, chip row 24px 정렬. |
| 컬러(15) | 15 | 카테고리 3색·점수 3단계·accent 이중역할 의미적, 충돌 0. |
| 타이포(15) | 15 | 한글 어절 줄바꿈·tabular-nums·위계 명확. |
| 일관성(10) | 10 | 토큰화 완결, 인라인 잔여는 데이터구동(threshold-dot)뿐. |
| 접근성(10) | 10 | 전 텍스트 AA(496요소 0 미달)·focus-visible·datetime·reduced-motion·color-scheme. |
| 반응형(10) | 9 | 데스크톱 2열/모바일 1열 깨짐 0, section-header wrap 보강. |

**최종 회귀 스윕:** ① 전체 페이지 텍스트 대비 스캔 — **AA 미달 0건**(버튼은 자기 배경 기준 재측정 반영). ② 모바일 375px 오버플로 0·1열·config 스택·header wrap. ③ 데스크톱 스크린샷 — 알림 카운트 블루/점수 red 분리, 버튼 가독성, 카테고리 일관성 시각 확인. ④ `pytest 82 passed`. ⑤ 계약: 3개 엔드포인트 `{items,total}`·9필드 불변.
**결론:** 95 베이스라인에서 측정 기반으로 5개 실결함(대비 4건·컬러 시맨틱 1건) 제거하여 **98** 도달. 후반(C3·6·7·8·10) 수렴 단계 — churn 없이 정밀 정제에 한정.

---

# ════ 라운드2 (사이클 11~30) ════

## 사이클 11 — 종합 98 (AAA 상향 시작)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | dim 텍스트 상향 후에도 primary(#e6edf3) vs dim(#a3acb8) 명도 차 유지 — 위계 보존 확인. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음(grey 계열 명도만 상향). |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 단일 토큰 변경으로 reason/meta/label/fb-btn/h-channel 일괄 상향(일관 유지). |
| 접근성(10) | 10 | **AAA 지향.** `--text-dim` AA→AAA. |
| 반응형(10) | 9 | 변화 없음. |

**적대적 점검:** 전 텍스트노드 대비 스캔(317노드) → 실제 텍스트 AA 미달 0(pill/badge ratio:1은 반투명 overlay 자기측정 아티팩트, 카드 불투명 bg 합성 시 5.0~8.5 통과 검증). `.dot`(2.31)은 aria-hidden 장식 — 제외 정당.
**계측 발견:** `--text-dim`(#8b949e)이 surface 대비 5.62 — AA는 통과하나 AAA(7.0) 미달. reason·meta·label·fb-btn·alert-badge-card·h-channel 등 다수 보조 텍스트가 여기 의존.
**적용 수정:** `--text-dim` #8b949e→#a3acb8 (bg 6.15→8.25, surface 5.62→7.54, surface-2 4.77→6.4 — bg/surface AAA 도달). overlay 위 사용처도 동반 상향: score-low 5.0→6.7, alert-badge-card/fb-btn/h-channel 4.77→6.4.
**재평가:** 리로드 — dim resolved rgb(163,172,184), primary rgb(230,237,243) 명도차 유지, 카드 23 렌더. `pytest 82 passed`.
**다음 후보:** btn-sm 모바일 44px 터치타깃, 스켈레톤 로딩 상태, 비텍스트 UI 대비(보더/포커스링) 3.0 점검.

---

## 사이클 12 — 종합 98→99 (터치타깃 결함 해소)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **라운드1 보류 결함 정면 해소.** WCAG 2.5.5 터치타깃. |
| 반응형(10) | 9→10 | 모바일 인터랙션 타깃 표준 충족. |

**계측 발견:** 모바일(375px) 실측 — `#send-btn`(btn-sm) 28px, `.fb-btn` 26px, `collect-btn` 35px 높이 → 모두 WCAG 2.5.5(AAA) / Apple HIG 44px 미달. 라운드1에서 "데스크톱 우선"으로 보류했던 항목.
**적용 수정:** `@media(max-width:640px)`에 `.btn/.btn-sm/.fb-btn { min-height:44px }` + 좌우 패딩 보정(세로만 늘려 라벨 가독 유지). 데스크톱은 불변(분기 한정).
**재평가:** 모바일 리로드 — send/collect/fb 모두 44px, docW=375(오버플로 0). 데스크톱 리로드 — send 28·fb 26 그대로(밀도 보존). `pytest 82 passed`.
**다음 후보:** 스켈레톤 로딩 상태(현재 초기 빈 화면), 비텍스트 대비(보더/포커스링 3.0), reduced-motion 실효 점검.

---

## 사이클 13 — 종합 99 (상태표면: 스켈레톤 로딩 추가)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 14 | 스켈레톤이 실제 카드와 동일 padding/gap·border-radius로 레이아웃 시프트 최소화. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | **상태표면 완성도↑.** 로딩/빈/에러 3종 모두 구비. |
| 접근성(10) | 10 | `aria-busy=true` 로딩 중 표시, `aria-hidden` 스켈레톤(스크린리더 무소음), reduced-motion 시 shimmer off. |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** 초기 로드 시 alerts/news 컨테이너가 fetch 완료까지 **빈 채로** 노출(empty-state도 미표시 구간) — 로딩 상태 표면 부재. 라운드 상향 기준의 "스켈레톤" 항목.
**적용 수정:** `.skeleton-card`(pill+title 2줄+meta) + shimmer 키프레임 추가. init에서 alerts 2·news 4개 즉시 렌더 후 데이터 도착 시 교체. `render()`가 `aria-busy` 해제. `load()`도 카드 부재 시에만 스켈레톤(재수집 시 깜빡임 방지). `prefers-reduced-motion`에서 shimmer 정지(정적 surface-2).
**재평가:** renderSkeleton 호출 → `#alerts .skeleton-card`=2, `aria-busy=true`, shimmer 애니메이션 `sk-shimmer` 활성. `load()` 후 카드23·스켈레톤0·aria-busy 해제 확인. `pytest 82 passed`.
**다음 후보:** 비텍스트 대비(카드 보더 1.x → 3.0?), 포커스링 가시성 진한 버튼 위, 발송 이력 스켈레톤.

---

## 사이클 14 — 종합 99 (비텍스트 대비: 포커스링 결함 해소)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **WCAG 1.4.11 포커스 표시 강화.** |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** 비텍스트 대비 스캔 — 카드/threshold/fb 보더(1.2~1.42)는 **장식(WCAG 1.4.11 예외, fill로 경계 식별)**이라 결함 아님. 그러나 `.btn:focus-visible` 포커스 링(`--accent` #4d9cf6)이 **primary 버튼 배경(`--accent-strong` #2563eb) 대비 1.82:1** — 포커스 표시는 1.4.11 적용 대상(3.0 필요)이라 진한 버튼 위 링 분리 미흡(실결함). (페이지 bg 대비는 6.67로 양호하나 버튼 자체 경계 약함.)
**적용 수정:** `.btn:focus-visible`에 `box-shadow:0 0 0 2px var(--bg)` 추가 — accent outline(offset 2px)과 버튼 사이에 어두운 갭을 넣은 2색 포커스링. 어느 배경에서도 링 가시.
**재평가:** collect-btn focus() — outline rgb(77,156,246) 2px·offset 2px·box-shadow rgb(13,17,23) 2px 확인. `pytest 82 passed`.
**다음 후보:** 발송 이력 로딩 스켈레톤, 4/8px 간격 그리드 이탈 스캔, prefers-color-scheme light 대응.

---

## 사이클 15 — 종합 99 (4px 그리드: 카드 gap 정렬)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 14→15 | **카드 내부 수직 리듬을 4px 그리드·스켈레톤과 통일.** |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 카드 gap=스켈레톤 gap=12px 동기화. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** 간격 스캔(17 셀렉터) — 구조 패딩이 18/14px 혼재. 대부분 의도적 마이크로 간격(3/6px pill·gap)이라 churn 회피. 단 `.card { gap:10px }`가 사이클13에서 추가한 `.skeleton-card { gap:12px }`와 불일치 → 로딩→로드 전환 시 미세 시프트.
**적용 수정:** `.card` gap 10→12px(4px 그리드, 스켈레톤과 동일). 다른 off-grid 값(comfortable padding 18/14)은 가독상 정당해 보존(churn 회피).
**재평가:** 리로드 — card gap 12px, 카드 23. `pytest 85 passed`(디스크상 신규 테스트 3건 포함 전부 green — 프론트 무영향 재확인).
**다음 후보:** 발송 이력 스켈레톤, prefers-color-scheme(라이트) 가드, 긴 제목 truncation/clamp.

---

## 사이클 16 — 종합 99 (상태표면 일관: 이력 스켈레톤)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 이력 스켈레톤 행이 실제 history-row와 동일 padding/gap. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | **3개 섹션(알림/뉴스/이력) 로딩 표면 완전 일관화.** |
| 접근성(10) | 10 | 이력도 `aria-busy`/`aria-hidden` 스켈레톤. |
| 반응형(10) | 10 | 변화 없음. |

**적대적 점검(무결 확인):** ① 카드 제목 라인 분포 1줄×10·2줄×13(max 2), 카드 높이 206~234px — 리듬 양호, **클램프 불필요**(뉴스 대시보드에서 제목 truncation은 정보 은닉=회귀라 의도적 미적용). ② reason 1줄×21·2줄×2 — 오버플로 없음. ③ `color-scheme:dark` 선언으로 UA 컨트롤/스크롤바 다크 일관, 폼 입력 부재로 라이트 강제 시 깨짐 없음.
**계측 발견:** 이력 섹션만 초기 로드 시 빈 컨테이너(알림/뉴스는 C13에서 스켈레톤화). 상태표면 비대칭.
**적용 수정:** `.skeleton-row`(time/title/channel 3분할) + `renderHistorySkeleton(3)` 추가, init·renderHistory에서 aria-busy 토글. shimmer는 기존 `.sk-line` 재사용(reduced-motion 자동 적용).
**재평가:** 리로드 — `#history .skeleton-row`=3·aria-busy=true → `loadHistory()` 후 history-row 8·스켈레톤 0·aria-busy 해제. `pytest 85 passed`.
**다음 후보:** aria-live 영역 실효(수집/발송 상태 announce), 카드 hover transform reduced-motion, 숫자 tabular 추가 점검.

---

## 사이클 17 — 종합 99 (aria-live 실효화)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **상태 announce 정확도↑.** |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** 수집/발송 상태 영역이 `aria-live=polite`는 있으나 `aria-atomic` 부재 → 스피너→완료 메시지 전환 시 부분 노드만 announce 위험. 또 ✓/✗ 상태 글리프가 SR에 읽혀 "체크표시 5건 수집 완료"처럼 잡음.
**적용 수정:** collect/send 상태에 `aria-atomic="true"`(전체 문구 단위 announce). 4개 status innerHTML의 ✓/✗ span에 `aria-hidden="true"`(스피너는 기존 hidden) → SR은 순수 메시지("5건 수집 완료")만.
**재평가:** 리로드 — 두 영역 aria-atomic=true·aria-live=polite 확인. `pytest 85 passed`.
**다음 후보:** 카드 hover translateY reduced-motion 무력화 확인, score-badge 외 숫자(카운트 배지) tabular, divider/empty 톤.

---

## 사이클 18 — 종합 99 (reduced-motion 완전화)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **WCAG 2.3.3 모션 완전 대응.** |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** reduced-motion 규칙이 `transition-duration`만 무력화 → `animation` 기반 **스피너(.spinner spin .7s infinite)**는 그대로 회전(vestibular 위험). 스켈레톤은 개별 처리됐으나 전역 누락.
**적용 수정:** 전역 reduced-motion 블록에 `animation-duration:.01ms`·`animation-iteration-count:1`·`scroll-behavior:auto` 추가 + `.card:hover{transform:none}`(호버 점프 제거).
**재평가:** 리로드 — 규칙 파싱 확인(animation-duration/iteration override + card hover none). `pytest 85 passed`.
**다음 후보:** 카운트 배지/이력 숫자 tabular, divider 톤, empty-state 카피·아이콘 정렬.

---

## 사이클 19 — 종합 99 (마이크로: 카운트 배지 tabular)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | **숫자 정렬 일관성 마무리.** |
| 일관성(10) | 10 | section-badge도 score-badge/threshold와 동일 tabular 정책. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** 섹션 카운트 배지(알림10·뉴스13·이력8)가 `font-variant-numeric:normal` — 자릿수 변동 시 폭 흔들림(score-badge는 R1에서 tabular화됨, 배지만 누락).
**적용 수정:** `.section-badge`에 `font-variant-numeric:tabular-nums`.
**재평가:** 리로드 — section-badge computed tabular-nums. `pytest 85 passed`.
**다음 후보:** divider 톤(현 --border), empty-state 아이콘/카피 광학정렬, 카드 reason 좌측 보더 위계 미세.

---

## 사이클 20 — 종합 99 (빈 상태 위계 복원)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | **빈 상태 주 메시지/힌트 2단 위계 복원.** |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | weight 500으로 주 메시지 강조. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 주 메시지 대비 상향(text-dim→text). |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** 사이클11의 text-dim 상향 부작용 — empty-state 주 메시지 `<p>`(상속 --text-dim)와 `.empty-hint`가 **동일 색(163,172,184)**이 되어 위계 소실(원래 둘 다 dim이었으나 이제 더 평면화).
**적용 수정:** `.empty-state p`에 `color:var(--text)`·`font-weight:500` — 주 메시지를 hint 위로. hint는 dim 유지.
**재평가:** 강제 빈 상태 — p rgb(230,237,243)/700→500, hint rgb(163,172,184) 2단 위계 확인 후 load() 복원. `pytest 85 passed`.
**다음 후보:** card-reason 좌측 보더 위계, divider 톤, source 텍스트 weight.

---

## 사이클 21 — 종합 99 (키보드: 건너뛰기 링크)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **WCAG 2.4.1 블록 건너뛰기.** |
| 반응형(10) | 10 | 변화 없음. |

**적대적 점검(무결 확인):** ① 탭 순서 — collect→send→카드 링크→피드백 3버튼→다음 카드… 94 focusable, **positive tabindex 0건**(안티패턴 없음), 트랩 없음, DOM=시각 순서 일치. ② reason 보더 차등(alert surface-3 / news border-soft) 의도적, source weight 500·full text — 정상.
**계측 발견:** sticky 헤더가 있는데 **skip-link 부재** — 키보드 사용자가 매번 헤더 버튼을 거쳐야 본문 도달(WCAG 2.4.1 권장 미충족).
**적용 수정:** `.skip-link`("본문으로 건너뛰기") 추가 — 평소 off-screen(top:-48px), `:focus` 시 top:12px로 슬라이드 인. `<main id="main-content" tabindex="-1">` 타깃. 첫 탭 스톱.
**재평가:** 리로드 — 첫 focusable=skip-link, focus 시 matches(':focus')·top 12px·activeElement 확인. `pytest 85 passed`.
**다음 후보:** divider 톤, 카드 hover box-shadow 비텍스트 대비, status 색(ok/err) 대비.

---

## 사이클 22 — 종합 99 (피드백 토글 상태 노출)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **WCAG 4.1.2/1.4.1 — 토글 상태 비색채 노출.** |
| 반응형(10) | 10 | 변화 없음. |

**적대적 점검:** status 색 대비 — ok-green 9.84(AAA)·err-red 6.84(AA)·error-banner 9.97(AAA), 게다가 ✓/✗ 아이콘+텍스트 병행이라 색 비의존. 카테고리(pill 색+라벨), 점수(색+숫자), 알림(보더+"발송 대상" 배지) 모두 색 외 단서 보유.
**계측 발견:** 피드백 버튼 active 상태가 **색+보더만**으로 표현 — 선택 결과가 SR에 미노출(`aria-pressed` 부재). 1.4.1(색 비의존)·4.1.2(상태) 갭.
**적용 수정:** 3개 fb-btn에 `aria-pressed="false"` 초기화, `sendFeedback`에서 선택 버튼 `true`/형제 `false` 토글(is-active 클래스와 동기).
**재평가:** 리로드 — fb-btn 69개 전부 aria-pressed 보유, 초기 false. `pytest 85 passed`.
**다음 후보:** divider 톤, config 패널 라벨 정렬, 카드 진입 stagger(있으면 reduced-motion), source·time 구분점 광학.

---

## 사이클 23 — 종합 99 (에러 status message 노출)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **WCAG 4.1.3 상태 메시지.** |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** `#global-error`(fetch/피드백 실패 배너)가 `role`/`aria-live` 부재 — 데이터 로드 실패 같은 치명 에러가 SR에 무음. error-banner 자체 대비는 9.97(AAA, 결함 아님)이나 announce 부재가 실결함.
**적용 수정:** `#global-error`에 `role="alert"` + `aria-live="assertive"` — 주입되는 배너 즉시 announce.
**재평가:** 리로드 — role=alert·aria-live=assertive 확인. showGlobalError 주입 시 컨테이너가 announce. `pytest 85 passed`.
**다음 후보:** config 패널 라벨/아이템 baseline 정렬, divider 톤, 카드 meta 구분점 광학 위치.

---

## 사이클 24 — 종합 99 (임계값 칩 SR 명료화)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **임계값 칩 SR 가독.** |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** 임계값 칩이 "글로벌 AI 8.5"로 토큰 분리 읽힘 — 단위/맥락 모호(dot은 이미 aria-hidden). 패널은 region+label 보유(양호).
**적용 수정:** `.threshold-item`에 `aria-label="${name} 임계값 ${val}점"`, 내부 name/value span은 `aria-hidden`(중복 읽기 방지). 시각 불변.
**재평가:** 리로드 — 3칩 aria-label "글로벌 AI 임계값 8.5점" 등, 시각 텍스트 "글로벌 AI 8.5" 유지. `pytest 85 passed`.
**다음 후보:** divider 톤, 카드 meta 구분점, 카드 hover 그림자 강도 일관.

---

## 사이클 25 — 종합 99 (외부 링크 고지)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **WCAG 3.2.5 새 창 고지.** |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** 카드 제목 링크가 `target="_blank"`(rel=noopener noreferrer 보안 양호)이나 새 탭 열림 사전 고지 없음 — SR/예측가능성 갭.
**적용 수정:** 링크에 `aria-label="${title} (새 탭에서 열림)"`. 시각 텍스트 불변(클러터 회피). 뉴스 카드 관례상 sighted엔 시각 아이콘 미추가(churn 회피).
**재평가:** 리로드 — aria-label에 "(새 탭에서 열림)" 부착, 가시 제목 동일. `pytest 85 passed`.
**다음 후보:** 카드 hover 그림자/보더 톤 일관, divider, lang 속성(영문 제목 mixed) 점검.

---

## 사이클 26 — 종합 99 (반응형 무결 스윕 360/768/1440)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | **3 뷰포트 적대적 검증 — 무결.** |

**적대적 점검(무결 확인):** ① 360px — docW=360(오버플로0)·1열·config 스택·send 44px. ② 768px — docW=753(오버플로0)·2열·send 28px(밀도 복귀). ③ 1440px — docW=1425(오버플로0)·3열(auto-fill minmax320)·container 1040px 캡 중앙정렬. 신규 요소(skip-link/스켈레톤/44px 규칙) 어느 폭에서도 깨짐 0.
**계측 발견:** divider 1.24:1(장식 — 40px 마진+섹션 타이틀이 분리 담당, 의도적 절제). 실결함 아님 — churn 회피.
**적용 수정:** 없음(무결 확인 사이클).
**다음 후보:** 영문/한글 혼용 제목 lang, 카드 진입 모션, 포커스 카드 스크롤.

---

## 사이클 27 — 종합 99 (이력 리스트 시맨틱 일관)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | **카드 그리드와 이력 리스트 role 통일.** |
| 접근성(10) | 10 | 이력 SR 리스트 탐색·항목수 노출. |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** 카드 그리드는 `role=list`/`listitem`인데 발송 이력은 평범한 div(리스트 시맨틱 부재) — SR 탐색/항목수 불일치.
**적용 수정:** `.history-list role="list"`, `.history-row role="listitem"`. (h-title CSS 말줄임은 DOM 텍스트 보존이라 SR 영향 없음·title 속성 툴팁 유지.)
**재평가:** 리로드 — list/listitem 8행 적용. `pytest 85 passed`.
**다음 후보:** mixed-language 제목 lang="en", 카드 포커스 시 스크롤 보정, 최종 회귀 스윕.

---

## 사이클 28 — 종합 99 (혼용 언어 lang 표기)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **WCAG 3.1.2 부분 언어.** |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** 페이지 `lang=ko`인데 영문 제목 11/23건 — 한국어 SR이 영문 헤드라인을 한글 발음으로 오독.
**적용 수정:** `detectLang(s)`(라틴≥4 AND 한글0 → 'en') 추가, 영문 판정 시 제목 링크에 `lang="en"`. 한글+영문약어 혼용("신한은행…AI")은 한글 보유로 미태깅(오태깅 방지). 데이터 스키마 무변경(클라 추정).
**재평가:** 리로드 — 23링크 중 11건 lang=en 정확 태깅, 한글 제목 default 유지. `pytest 85 passed`.
**다음 후보:** 최종 회귀: 전 요소 대비 재스캔, 키보드 일주, 계약 필드 재확인.

---

## 사이클 29 — 종합 99 (최종 회귀 스윕 · 무결)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **AAA 비율 65% 도달 확인.** |
| 반응형(10) | 10 | 변화 없음. |

**적대적 점검(무결 확인):** ① 전 텍스트노드 정밀 대비 재스캔(aria-hidden 제외, 다층 알파 합성) → **287노드 / AA 미달 0 / AAA(7.0) 187(65%) / AA-only 100**. AA-only는 의도적 시맨틱 색(카테고리 pill·점수 배지)과 가장 어두운 pill 위 dim 텍스트 — 정당. ② 계약: alerts/news/history 모두 `{items,total}`, 11개 필드(importance_score…category_label) 전부 존재 — 데이터/스키마 무변경. ③ `pytest 85 passed`.
**적용 수정:** 없음(회귀 0 확인 사이클).
**다음 후보:** 최종 마이크로(스켈레톤↔실데이터 시프트 0 확인), 종합 마감.

---

## 사이클 30 — 종합 99 (CLS 저감 · 라운드2 마감)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | **로딩→로드 레이아웃 시프트 저감.** |

**계측 발견:** 스켈레톤 카드 높이 134px vs 실제 카드 206~234px — 로드 시 상향 시프트(CLS).
**적용 수정:** 스켈레톤에 `.sk-reason`(32px 블록) 1줄 추가 → 134→178px로 실카드 근사. 시각 일관·시프트 저감.
**재평가:** 스켈레톤 높이 178px 확인, 데스크톱 스크린샷 — 전 섹션 정상, 회귀 0. `pytest 85 passed`.
**라운드2 결론:** 98→99 수렴. 실결함 9건(터치타깃·포커스링·reduced-motion·aria-live·role=alert·aria-pressed·skip-link·lang·empty위계) + AAA 상향(text-dim) + 상태표면 완성(스켈레톤 3종) 적용. AAA 비율 65%·AA 미달 0·계약 불변·85 passed.

---

## 사이클 1 — 종합 95

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24 | 알림(색보더+발송배지+점수) vs 전체(평면) 위계 확립. |
| 간격(15) | 14 | 그리드·divider 대칭 리듬. |
| 컬러(15) | 14 | 카테고리 3색·점수 3단계 의미적. |
| 타이포(15) | 15 | 한글 어절 줄바꿈, 위계 명확. |
| 일관성(10) | 10 | 토큰화·칩 정렬 통일. |
| 접근성(10) | 9→10 | **결함 발견·수정.** |
| 반응형(10) | 9 | 데스크톱 2열/모바일 1열 깨짐 없음. |

**계측 발견:** computed-style 대비 측정 결과 `.fb-label`("피드백" 텍스트)가 `--text-dimmer`(#4d5566)로 surface 대비 **2.31:1** — WCAG AA(4.5:1) 미달. 이건 장식이 아니라 실제 라벨 텍스트라 명확한 결함. (참고: `.fb-btn` 4.77, `.card-reason` 5.62, `.card-meta .source` 14.64는 통과.)
**적용 수정:** `.card-feedback .fb-label` 색 `--text-dimmer`→`--text-dim`(#8b949e).
**재평가:** 리로드 후 `.fb-label` 대비 **5.62:1**, AA 통과. 알림 10/뉴스 13 렌더 유지, 회귀 없음.
**다음 후보:** 점수배지 score-low 텍스트 대비(2.43 측정), 빈 상태 empty-hint 대비, 반응형 헤더 wrap 정밀 점검.

---

## 사이클 2 — 종합 96

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24→25 | 알림 카운트 배지 색 재정의로 red 시그널 정리. |
| 간격(15) | 14 | chip row 24/24/24 정렬·섹션 40px 리듬 계측 확인. |
| 컬러(15) | 14→15 | **결함 발견·수정.** |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 키보드 focus-within 동작 확인(링크 포커스→카드 링), 94 focusable 도달. |
| 반응형(10) | 9 | 변화 없음. |

**계측 발견:** ① score-low 텍스트는 실제 효과 배경(badge bg over card) 기준 **4.98:1**로 통과(앞 측정은 투명 배경 오판) — 결함 아님. ② 알림 섹션 카운트 배지가 `--score-high`(#f87171, 빨강) = 고점수 배지와 **완전 동일 색**. 이 대시보드에서 빨강 = "중요도 8–10". 단순 개수에 빨강을 쓰면 점수 시그널과 의미 충돌.
**적용 수정:** `.section-badge.alert-badge` 배경/글자를 `--accent-dim`/`--accent`(블루)로. 블루는 이미 알림 정체성(좌측 보더·발송배지 도트) 색이라 의미 일관. 빨강은 이제 고점수 전용.
**재평가:** 리로드 후 카운트 배지 블루, 대비 **5.68:1** AA 통과. 알림 10 렌더 유지, 회귀 없음.
**다음 후보:** 반응형(모바일 헤더/설정패널 스택) 정밀 계측, history-row 모바일 wrap, score-mid 과다(19/23) 시 위계 희석 점검.

---

## 사이클 3 — 종합 96 (수렴 단계)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 9 | **잠재 결함 보강.** 375px에서 doc scrollWidth=375(오버플로 0), 1열 그리드, config column 스택, history-row wrap 모두 정상. |

**계측 발견:** 모바일(375px)에서 큰 깨짐 없음. 단 알림 섹션 `.section-header`가 `flex-wrap:nowrap`이라 타이틀 블록과 발송 액션 그룹이 한 행 고정 — 현재 53px 여유지만 타이틀이 길어지면 발송 버튼을 압박할 구조적 취약점.
**적용 수정:** mobile media query에 `.section-header { flex-wrap: wrap; }` 추가. 액션 그룹의 인라인 `margin-left:auto`는 wrap 시 자기 행 우측 정렬로 자연 동작(특이성 충돌 회피 위해 override 룰은 제거).
**재평가:** 모바일 리로드 — wrap 활성·오버플로 0·알림 10 유지. 데스크톱 리로드 — 2열 그리드·section-header nowrap·오버플로 0, **회귀 없음**.
**다음 후보:** 점수 위계(score-mid 19건 과밀)에서 알림 카드 고점수 강조, history h-channel pill 대비, 카드 호버 리프트 모션 일관성.

---

## 사이클 4 — 종합 96 (수렴 단계)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 알림 점수 분포(9~4.8) 확인. score color는 절대 척도(8+/5+/<5)로 의도된 전역 시맨틱 — 알림 4.8이 grey여도 '발송 대상' 배지+좌측 보더가 알림 신호를 별도 전달하므로 의도 충돌 아님(churn 회피). |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음(시각 동일). |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | **결함 보강.** |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 9 | 변화 없음. |

**계측 발견:** 코드 스캔 — `.score-badge.score-low { color:#8b949e }`가 `--text-dim`(#8b949e)과 **값 동일한 raw 리터럴**. 토큰 변경 시 이 배지만 조용히 디버지(divergence)할 일관성 취약점. (line 459)
**적용 수정:** `#8b949e` → `var(--text-dim)`. 시각 변화 0, 토큰 단일 출처화.
**재평가:** 리로드 — score-low 색 rgb(139,148,158) 동일, 알림 10/뉴스 13 유지, 회귀 없음.
**다음 후보:** 남은 리터럴(`#fff` btn-primary) 검토, history h-channel/empty 상태 미세 대비, 카드 reason 좌측 보더 위계.

---

## 사이클 5 — 종합 97

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 9→10 | **결함 발견·수정.** (직전 10에서 추가 결함 제거로 견고화) |
| 반응형(10) | 9 | 변화 없음. |

**계측 발견:** `--text-dimmer`(#4d5566) 3개 사용처 점검 — `.card-meta .dot`은 장식(aria-hidden, 유지 정당), `.score-null`은 희소. 그러나 `.empty-state .empty-hint`("수집 실행 버튼을 눌러…")는 **빈 상태에서 사용자가 가장 의지하는 안내 텍스트**인데 페이지 bg 대비 **2.53:1**로 AA 미달.
**적용 수정:** `.empty-hint` 색 `--text-dimmer`→`--text-dim`. (--text-dim on bg = 6.15:1)
**재평가:** 빈 상태 강제 렌더 후 computed color rgb(139,148,158) 확인 → **6.15:1** AA 통과, 이후 `load()`로 데이터 복원(알림10/뉴스13).
**다음 후보:** 카드 판단근거 좌측 보더 위계, h-channel pill 대비, 호버/포커스 모션 일관성 미세조정.

---

## 사이클 6 — 종합 97 (수렴 단계 · 마이크로 정제)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | reason 보더(알림=surface-3 / 뉴스=border-soft) 차등 정상, score badge 36px 균일 정렬 확인. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | **마이크로 정제.** |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 9 | 변화 없음. |

**검증:** `pytest 82 passed` — 프론트 변경이 백엔드/계약에 무영향. score badge 폭 전부 36px(`7.9`,`6.5` 등 가변 텍스트에도 균일), reason 보더 차등 의도적.
**계측 발견:** 점수배지·임계값이 비례 숫자(proportional)라 `6.5`/`7.9` 같은 소수 폭이 미세하게 흔들림 — 데이터 readout 성격에 어긋남.
**적용 수정:** `.score-badge`·`.threshold-value`에 `font-variant-numeric: tabular-nums`. 숫자 폭 고정으로 정렬 정밀화(레이아웃 영향 0).
**재평가:** 리로드 — 두 셀렉터 computed `tabular-nums`, 알림 10 유지.
**다음 후보:** h-channel pill 대비(4.77), 카드 호버 리프트 모션 일관성, divider 톤.

---

## 사이클 7 — 종합 97 (수렴 단계 · 마이크로 정제)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 카드와 동일하게 이력 시각도 `<time>` 시맨틱 통일. |
| 접근성(10) | 10 | machine-readable datetime을 이력 행에도 부여. |
| 반응형(10) | 9 | 변화 없음. |

**계측 발견:** 발송 이력 8행 렌더 정상(cat pill·time·channel "console"·ellipsis). h-channel pill 4.77:1로 AA 통과(결함 아님). 단 카드 메타는 `<time datetime>`인데 이력 시각은 평범한 `<span>` — 시맨틱 불일치. 또 `renderHistory`에 미사용 변수 `label` 잔존.
**적용 수정:** 이력 시각을 `<time class="h-time" datetime="${sent_at}">`로(빈 값 시 "—" 폴백), dead var `label` 제거. `.h-time`는 클래스 셀렉터라 스타일 그대로 적용.
**재평가:** 리로드 — `time.h-time` 존재·datetime=ISO·색 동일(rgb139,148,158)·8행 유지. `pytest 82 passed`.
**다음 후보:** 카드 호버 리프트 모션 일관성, score-null/error 배너 상태, divider 톤 미세.

---

## 사이클 8 — 종합 97 (수렴 단계)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **잠재 상태 결함 보강.** |
| 반응형(10) | 9 | 변화 없음. |

**계측 발견:** 상태 시뮬레이션 — error-banner 대비 **8.85:1**(양호). 그러나 `importance_score:null` 카드의 `.score-null` 배지("–")가 `--text-dimmer` on surface-2로 **1.96:1**, 거의 안 보임(AA 대실패). 희소 상태지만 실제 렌더되는 UI.
**적용 수정:** `.score-badge.score-null` 색 `--text-dimmer`→`--text-dim`. (surface-2 대비 4.77:1)
**재평가:** 널 카드 강제 렌더 후 대비 **4.77:1** AA 통과, `load()`로 복원(알림10/뉴스13).
**다음 후보:** 마지막 마이크로 — 호버 모션/divider 톤/포커스 링 일관성 최종 점검.

---

## 사이클 9 — 종합 98

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | accent를 텍스트/보더용(밝음)과 버튼배경용(진함)으로 역할 분리 — 시맨틱 정교화. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 9→10 | **루프 중 최대 결함 수정.** |
| 반응형(10) | 9 | 변화 없음. |

**계측 발견:** 전체 페이지 텍스트 노드 대비 스캔 → primary 버튼("수집 실행"/"발송") 흰 글자가 `--accent`(#4d9cf6) 배경 위 **2.84:1** — primary CTA가 AA 미달. (이 루프에서 발견한 가장 큰 결함.) `#fff` 리터럴도 여기 잔존.
**적용 수정:** `--accent-strong: #2563eb` 토큰 신설, `.btn-primary` 배경에 적용(흰글자 5.17:1). `--accent`(#4d9cf6)는 보더/포커스링/텍스트용으로 유지 — 포커스 링은 진한 버튼 위에서 여전히 가시(outline rgb(77,156,246)).
**재평가:** 리로드 — 두 버튼 5.17:1 AA 통과, 포커스 링 정상, 로고 그라디언트 무영향, 알림 10. `pytest 82 passed`.
**다음 후보:** 수렴 — 최종 회귀 스윕(데스크톱/모바일), 잔여 텍스트 대비 0 확인.

---

# ════ 라운드3 — 모바일 우선 (사이클 31~40) ════

## 사이클 31 — 모바일 종합 88 (sticky 헤더 뷰포트 예산 결함)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 23 | 변화 없음(헤더 콘텐츠 우선순위는 유지). |
| 간격(15) | 13 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 14 | 모바일 h1 17→16px(헤더 컴팩트 동반). |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 9 | 수집 버튼 44px 터치타깃 유지(헤더 컴팩트 후에도). |
| 반응형(10) | 8→9 | **모바일 sticky 헤더 과점 해소.** |

**계측 발견(320px):** `.header-inner { flex-wrap:wrap }`로 인해 좁은 폭에서 수집 버튼이 제목 아래로 줄바꿈 → **헤더 높이 125px(뷰포트 720의 17%)**. sticky라 스크롤 내내 콘텐츠 17% 가림(뷰포트 예산 과점). subtitle도 좁은 폭에서 줄바꿈 유발.
**적용 수정:** `@media(max-width:640px)`에 모바일 헤더 컴팩트 — `.header-inner{flex-wrap:nowrap;padding 14→10px}`, `.header-title{min-width:0}`+`h1{white-space:nowrap;ellipsis;16px}`, `.subtitle{display:none}`, `.header-logo 32→28px`, `.header-actions{flex-shrink:0}`. 한 행 고정으로 줄바꿈 제거.
**재평가:** 320px 리로드 — 헤더 **125→65px(17%→9%)**, wrapped=false, 오버플로 0, collect 44px 유지. 데스크톱(1440) — 헤더 75px·subtitle block·h1 17px·3열·오버플로 0 **회귀 0**.
**다음 후보:** 피드백 버튼 줄바꿈 시 행간(row-gap 6px<8px) 탭타깃 간격, 섹션헤더 발송버튼 좁은폭 정렬, 유체 타이포.

---

## 사이클 32 — 모바일 종합 89 (줄바꿈 탭타깃 간격 결함)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 23 | 변화 없음. |
| 간격(15) | 13→14 | **모바일 탭타깃 간격 표준화.** |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 14 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 9→10 | **WCAG 2.5.8 타깃 간격 정신.** |
| 반응형(10) | 9 | 변화 없음. |

**계측 발견(320px):** 피드백 3버튼이 좁은 폭에서 2행 줄바꿈 → 행간 `row-gap:6px`. 인접 44px 탭타깃 사이 수직 간격 6px(<8px)는 모바일 오탭 위험(WCAG 2.5.8 / Material·HIG 권장 8px↑).
**적용 수정:** 모바일 분기에 `.card-feedback { gap:8px }`(col·row 동시 8px). 데스크톱은 6px 유지(밀도 보존).
**재평가:** 320px 리로드 — row-gap 8px·vGap 8px·오버플로 0. 데스크톱(1440) — fb row-gap 6px·3열·오버플로 0 **회귀 0**.
**다음 후보:** 섹션헤더 발송버튼 좁은폭 정렬, 유체 타이포(clamp), 중간 브레이크포인트(태블릿 2열).

---

## 사이클 33 — 모바일 종합 90 (태블릿 awkward zone: 단일 초대형 카드)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 23 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 14 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 9→10 | **중간 브레이크포인트 갭 해소(640~676px 단일 620px 초대형 카드).** |

**계측 발견(660px):** mobile 분기(≤640) 밖이라 `minmax(320,1fr)` auto-fill 적용 — 콘텐츠폭 620px에 320×2+16gap=656>620으로 **1열만 생성, 카드 620px 폭**(데스크톱 75px 헤더와 함께 가로 공간 낭비·초대형 단일 카드). 641~676px 구간 공통 결함.
**적용 수정:** `.card-grid` minmax floor `320→288px`. 콘텐츠 ~592px(=폭 ~632)부터 2열 확보. 데스크톱(1040 content=1000 / 1440 cap=1000)은 여전히 3열(3×288+2gap=896<1000) — 불변.
**재평가:** 660px 리로드 — **1열620 → 2열302**·오버플로 0. 1040/1440 — 3열 유지(카드 323px)·오버플로 0 **회귀 0**. 768 — 2열(앞서 확인) 유지.
**다음 후보:** 유체 타이포(clamp 제목/섹션), 섹션헤더 발송버튼 좁은폭, safe-area/landscape.

---

## 사이클 34 — 모바일 종합 91 (safe-area: 노치·가로모드 침범 결함)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 23 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 14 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | **노치/홈인디케이터 safe-area 대응(가로모드 가독).** |

**계측 발견:** 뷰포트 메타에 `viewport-fit=cover` 부재 → `env(safe-area-inset-*)` 항상 0(미사용). 노치 단말 **가로모드**에서 container/header가 고정 14~20px만 확보 → 콘텐츠가 노치·둥근모서리 아래 가려질 위험. 하단 홈인디케이터도 미고려.
**적용 수정:** `viewport-fit=cover` 추가 + `.container`·`.header-inner`(데스크톱·모바일 분기 모두) 좌우 패딩을 `max(base, env(safe-area-inset-*))`로(가산·비축소), `main` 하단 `calc(48px + env(safe-area-inset-bottom))`. inset 0 단말은 base값 그대로(비노치 무영향).
**재평가:** 320px(inset0) — container 14px·main-pb 48px·오버플로 0. 1440 — container 20px·3열·오버플로 0 **회귀 0**. `pytest 85 passed`.
**다음 후보:** 유체 타이포(clamp), 섹션헤더 발송버튼 좁은폭 정렬, sticky 헤더 하단 콘텐츠 스크롤 여백.

---

## 사이클 35 — 모바일 종합 92 (유체 타이포: 섹션 제목 하드 점프 제거)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 23→24 | **섹션 제목이 폭에 따라 매끄럽게 성장 — 위계 안정화.** |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 14→15 | 유체 스케일(clamp) 도입. |
| 일관성(10) | 10 | 하드 분기 1개 제거(분기 단순화). |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** `.section-title`이 데스크톱 22px / 모바일 하드 18px → **640px 경계에서 4px 즉시 점프**(큰 폰 414px에서도 18px 고정, 작은폰과 동일·성장 없음). 유체 부재.
**적용 수정:** `.section-title { font-size: clamp(18px,4.2vw,22px) }` + 모바일 하드 `18px` 오버라이드 제거. 작은폰 18px 바닥·~524px부터 22px·데스크톱 22px 캡(밀도 보존).
**재평가:** 360/390/414 → 18px(바닥), 1440 → **22px(캡 유지)**·3열·오버플로 0 **회귀 0**. 모든 폭 오버플로 0.
**다음 후보:** 섹션헤더 발송버튼 좁은폭 정렬, sticky 헤더 scroll-margin(앵커), 카드 메타 줄바꿈.

---

## 사이클 36 — 모바일 종합 93 (섹션헤더 발송액션 좁은폭 줄바꿈 견고화)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | **긴 발송 상태문구의 좁은폭 오버플로 구조적 예방.** |

**계측 발견(320px):** 알림 섹션헤더의 `.header-actions`(발송 상태문구+버튼)가 `flex-wrap:nowrap` → 자기 행 안에서 긴 실패/완료 문구가 들어오면 버튼을 압박, 우측 잘림 위험. 짧은 문구는 통과하나 "발송 실패: …" 류 긴 문구에서 취약(구조적).
**적용 수정:** 모바일 분기에 `.section-header .header-actions { flex-wrap:wrap; justify-content:flex-end }` — 상태문구가 버튼 행과 분리되어 우측 정렬 유지하며 자연 줄바꿈. (헤더 수집 액션은 단일 버튼이라 영향 없음.)
**재평가:** 320px — 긴 실패 문구 주입 시 오버플로 0(우측 253<306), wrap 동작. 데스크톱(1440) — 섹션 header-actions nowrap 유지·3열·오버플로 0 **회귀 0**.
**다음 후보:** sticky 헤더 앵커 scroll-margin-top, 카드 메타 줄바꿈 품질, 발송이력 row 모바일 스택.

---

## 사이클 37 — 모바일 종합 94 (sticky 헤더 앵커 가림 결함)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **앵커/포커스 스크롤 타깃이 sticky 헤더에 가리지 않음(2.4.x 가시성).** |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견(390px):** `#main-content`·섹션 제목·카드의 `scroll-margin-top` 모두 0. 65px(모바일)/75px(데스크톱) sticky 헤더가 있어 skip-link 점프(#main-content)나 카드 포커스 스크롤 시 타깃이 헤더 **아래로 65~75px 가림**(상단 잘림).
**적용 수정:** `#main-content, .section-title, .card { scroll-margin-top: 88px }`(최대 헤더 75+여백 13 커버). 앵커 도착 지점이 헤더 하단 아래로 내려옴.
**재평가:** 390px — 마지막 카드 scrollIntoView 시 top 88>headerBottom 65(clears). 데스크톱(1440) — top clears 75·3열·오버플로 0 **회귀 0**.
**다음 후보:** 카드 메타 줄바꿈 품질, 발송이력 row 모바일 스택, score 배지 float 충돌.

---

## 사이클 38 — 모바일 종합 95 (발송이력 row 모바일 스택)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24→25 | **이력 제목이 전체폭 1행 우선 — 모바일 가독·위계 회복.** |
| 간격(15) | 14→15 | 행높이 132→97px, 4px 그리드 gap. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | h-title weight 500(모바일 강조). |
| 일관성(10) | 10 | 카드와 동일하게 제목 우선 위계. |
| 접근성(10) | 10 | DOM 순서 불변(order는 시각만) — SR 읽기 순서 유지. |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견(360px):** 이력 row가 `[시각118][제목110][채널58]` 3열로 360px에 압축 → 제목이 110px만 받아 4줄 줄바꿈·row 132px(난잡). 기존 모바일 분기는 `flex-wrap`만 했지 제목 폭 확보 없음.
**적용 수정:** 모바일 분기 — `.h-title { order:-1; flex:1 0 100%; white-space:normal; weight:500 }`(제목 전체폭 1행 선두), 시각+채널은 그 아래 메타 1행. row gap `4px 10px`, `align-items:baseline`.
**재평가:** 360px — 제목 298px 전체폭(top 6683)·시각/채널 하단 메타·row **132→97px**·오버플로 0. 데스크톱(1440) — time→title→channel 동일행·nowrap·오버플로 0 **회귀 0**. `pytest 85 passed`.
**다음 후보:** 카드 상단행 점수배지↔카테고리 충돌, 카드 메타 줄바꿈, 컨트롤 스택 최종 점검.

---

## 사이클 39 — 모바일 종합 96 (설정 패널 칩 가로 줄바꿈 / 카드상단 무결 확인)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 설정 패널 158→96px(수직 공간 절약). |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | **컨트롤 스택 효율화.** |

**적대적 점검(무결 확인, 320px):** ① 알림 카드 상단행 — 카테고리 pill(우측 99~123) ↔ card-top-right(점수+발송배지, 좌측 172) 사이 ~50px 여유, wrap=false·충돌 0·오버플로 0(10카드 전수). ② 카드 메타 — 최대 source 173px(카드폭 내), 23개 중 2개만 2줄 우아한 줄바꿈·오버플로 0. → 카드 내부 재배치 **결함 없음**.
**계측 발견:** 모바일 설정 패널이 `flex-direction:column`으로 3칩(각 ~115px)을 세로 스택 → 패널 158px(수직 낭비). 칩이 작아 가로 줄바꿈이 더 효율적.
**적용 수정:** 모바일 `.config-panel`에서 `flex-direction:column`·`align-items:flex-start` 제거(베이스 `flex-wrap:wrap`·`align-items:center` 상속). 라벨+칩 가로 흐름·줄바꿈.
**재평가:** 320px — 칩 2행 가로 흐름·패널 **158→96px**·오버플로 0. 데스크톱(1440) — 1행·59px·오버플로 0 **회귀 0**.
**다음 후보:** 최종 다중폭 회귀 스윕(320·360·390·414·768·1040·1440)·landscape.

---

## 사이클 40 — 모바일 종합 96 (최종 다중폭 + 가로모드 회귀 스윕 · 수렴)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | **7폭+가로모드 전수 무결.** |

**적대적 점검(전 폭 계측, 데이터 알림10·뉴스13·이력8 적재 상태):**

| 폭 | 오버플로 | 열 | 헤더 | 섹션제목 |
|----|---------|----|------|---------|
| 320 | 0 | 1 | 65px | 18px |
| 390 | 0 | 1 | 65px | 18px |
| 414 | 0 | 1 | 65px | 18px |
| 844(가로) | 0 | 2 | 75px | — |
| 768 | 0 | 2 | 75px | — |
| 1040 | 0 | 3 | 75px | 22px |
| 1440 | 0 | 3 | 75px | 22px |

**적용 수정:** 없음(회귀 0 확인 사이클).
**검증:** ① 7폭+landscape(844×390) 전부 수평 오버플로 0. ② 데스크톱(1040/1440) — 3열·헤더75·subtitle노출·제목22px **회귀 0**. ③ `pytest 85 passed`. ④ 계약: `/api/alerts` `{items,total}`·11필드(importance_score…sent_at) 전부 존재·결손 0. ⑤ src/web/index.html 프레젠테이션만 수정.
**라운드3 결론:** 모바일 우선 관점에서 실결함 7건 제거(헤더 뷰포트과점·탭타깃간격·태블릿 awkward 1열·safe-area·유체타이포 하드점프·섹션액션 오버플로·이력 모바일 스택) + 무결 카드상단/메타 확인. 데스크톱 회귀 0.

---

## ★★★ 라운드3 최종 요약 (사이클 31~40 완주)
- **모바일 종합 점수 추이(31→40):** 88 · 89 · 90 · 91 · 92 · 93 · 94 · 95 · 96 · 96 → **최종 96/100**
- **제거한 모바일 실결함 7건:**
  ① **sticky 헤더 뷰포트 과점**(125px/17%→65px/9%, 컴팩트 1행·subtitle 숨김)
  ② **줄바꿈 탭타깃 간격**(피드백 row-gap 6→8px, WCAG 2.5.8)
  ③ **태블릿 awkward zone**(640~676px 단일 620px 초대형 카드 → minmax 320→288로 2열)
  ④ **safe-area 미대응**(viewport-fit=cover + container/header `max(base,inset)` + main `+inset-bottom`)
  ⑤ **유체 타이포 하드점프**(섹션제목 18↔22 즉시점프 → clamp(18,4.2vw,22))
  ⑤ **섹션 발송액션 오버플로**(긴 상태문구 → 모바일 header-actions flex-wrap)
  ⑦ **발송이력 모바일 스택**(제목 110px 4줄압축 → 전체폭 1행, row 132→97px)
  + 설정패널 칩 가로줄바꿈(158→96px), sticky 헤더 앵커 scroll-margin-top 88px.
- **무결 확인(지어내지 않음):** 카드 상단행(pill↔점수배지 ~50px 여유·충돌0)·카드 메타 줄바꿈(우아) — 결함 없어 churn 회피, 수치로 통과 기록.
- **데스크톱 회귀 0:** 매 사이클 1040/1440 동시 계측 — 3열·헤더75px·subtitle노출·제목22px·container20px 불변, 7폭+가로모드 오버플로 0.
- **검증:** `pytest 85 passed`(전 사이클)·계약 `{items,total}`·11필드 불변·src/web/만 수정.
- **남은 한계:** ① 844×390 가로모드는 width>640이라 데스크톱 헤더(75px=화면높이 19%) 사용 — 폭이 넓어 깨짐은 없으나 짧은 높이용 헤더 압축은 미적용(landscape+저높이 전용 미디어쿼리는 over-engineering 판단, 보류) ② 카드 제목 모바일 16/17px는 데스크톱과 동일 — 유체화 검토했으나 한글 가독 하한이라 유지 ③ clamp vw 단위는 사용자 줌과 독립(루트 rem 기반 clamp는 차기 검토 여지).

---

# ════ 라운드4 — 탭 UI 데스크톱 (사이클 41~50) ════

## 사이클 41 — 데스크톱 종합 93 (sticky 탭바 겹침 + 개요그리드 빈 컬럼)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 23 | 개요 위계는 양호(스탯28>알림17>미니헤드15>미니아이템13). 단 우측 이력 데드 화이트스페이스. |
| 간격(15) | 13→14 | **개요 2컬럼 정렬 회복.** |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 9→10 | sticky 스택 일관(헤더↔탭바 도킹). |
| 접근성(10) | 10 | scroll-margin 헤더+탭바 커버로 앵커 가림 해소. |
| 반응형(10) | 9→10 | **탭바 도킹·개요 2열/1열 분기.** |

**계측 발견(1280/1440):** ① `.tabs { top:61px }`인데 데스크톱 헤더 실측 **75px** → 스크롤 시 탭바가 헤더 아래로 **14px 겹쳐 가려짐**(gapOrOverlap −14). 61px는 모바일(헤더 53px)용 값이 데스크톱에 잘못 적용. ② 개요 `.overview-grid { auto-fill minmax(320,1fr) }` → 콘텐츠 1000px에서 **3열 생성**(자식 2개) → **3번째 컬럼 통째 공백**(323px) + 좌측 카테고리(751px) 대비 우측 이력(280px)에 **471px 데드 화이트스페이스**. 빌드노트의 "우측 이력 여백" 문제 실체.
**적용 수정:** ① `.tabs top:61→75px`(데스크톱 헤더에 정확 도킹), `scroll-margin-top 88→132px`(헤더75+탭바45). ② `.overview-grid`를 명시 2열 `minmax(0,1.5fr) minmax(0,1fr)`(카테고리 넓게|이력 사이드바) + `@media(max-width:860px){1fr}`(좁은폭 스택).
**재평가:** 1280 — 스크롤 시 **gapOrOverlap −14→0**(탭바 헤더에 flush 도킹). 개요그리드 **3열→2열**(590|394px), 빈 컬럼 제거·카테고리 615px(751→축소). docW 1285(오버플로0). `pytest 85 passed`.
**다음 후보:** 와이드(1920) 카드열 상한, 탭 활성 인디케이터 위계, 스탯/미니 위계 정밀.

---

## 사이클 42 — 데스크톱 종합 94 (와이드 카드열·탭 위계 무결 확인)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 23→24 | **탭 활성 위계·개요 타이포 스케일 무결 확인.** |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | 와이드(1920) 카드열 상한 검증. |

**적대적 점검(무결 확인, 지어내지 않음):**
① **와이드 카드열 상한(1920px):** 빌드노트 우려(minmax288→4열)와 달리 `.container max-width:1040px` 캡으로 콘텐츠 1000px → **전 탭 3열·카드 323px 유지**(1280·1440·1920 동일). 4열 미발생 — 컨테이너 캡이 가독폭 보호. 결함 아님.
② **탭 활성 위계:** 활성 탭 color rgb(230,237,243)+언더라인 accent 2px / 비활성 dim rgb(163,172,184)+언더라인 투명. 색+언더라인 이중 단서(색 비의존 1.4.1 충족). tablist 좌측 x=193=container x(정렬 일치). 결함 아님.
③ **개요 타이포 스케일:** 스탯값28 > 알림제목17 > 미니헤드15 > 미니아이템13 — 명확한 하강 위계. 결함 아님.
**적용 수정:** 없음(무결 확인 사이클 — 수치 기록).
**다음 후보:** 탭 전환 시 패널 포커스/스크롤, 카테고리별 필터칩 활성 위계, 중요뉴스 그룹헤더 위계.

---

## 사이클 43 — 데스크톱 종합 95 (카테고리 필터칩 활성 상태 의미 강화)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | **활성 칩 보더에 카테고리 색 반영 — 컬러 시맨틱 정교화.** |
| 타이포(15) | 15 | chip-n tabular-nums 토큰화. |
| 일관성(10) | 9→10 | 인라인 `opacity:.7` 카운트 → `.chip-n` 클래스화. |
| 접근성(10) | 10 | 활성 상태가 배경+**색보더** 이중 단서(색 비의존 보강). |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견(by-category 탭):** 카테고리 필터칩 활성(`aria-pressed=true`) 상태가 **배경(surface→surface-3) + 보더 그대로(30363d)**만으로 표현 — 선택 칩이 카테고리 정체성 색을 전혀 안 띰(도트만 색). 활성/비활성 구분이 미약한 배경 리프트에만 의존(약한 위계). 또 카운트 숫자가 인라인 `style="opacity:.7"`(토큰화 미흡).
**적용 수정:** ① 칩에 `--chip-c:${카테고리색}` 인라인 변수 주입 → `.cat-chip[aria-pressed=true]` 보더를 `var(--chip-c, var(--accent))`로(활성 칩이 카테고리 색 보더, "전체"는 accent 폴백). ② 인라인 카운트 → `.chip-n` 클래스(text-dim·tabular-nums·활성 시 text). 
**재평가:** global_finance_ai 활성 → 보더 **에메랄드 rgb(52,211,153)**, global_ai → **보라 rgb(167,139,250)**, 전체 → **accent rgb(77,156,246)** 폴백 확인. `pytest 85 passed`.
**다음 후보:** 탭 전환 시 패널 스크롤 위치, 중요뉴스 빈 그룹 처리, 스탯 카드 0건 카테고리 노출 정책.

---

## 사이클 44 — 데스크톱 종합 95 (탭 전환 시 스크롤 위치 복원)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 탭 전환 후 새 패널 제목이 뷰포트 내(탭바 하단)에 보임 — 방향감 유지. |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견:** 개요 탭에서 깊이 스크롤(y=1154) 후 "전체뉴스"로 전환 → 스크롤 y=675 **유지** → 새 패널 제목(h2Top −530)·첫 카드(−450)가 **뷰포트 위로 잘림**. 사용자가 새 탭의 중간 콘텐츠에 떨어져 제목 보려면 위로 스크롤해야 함(방향 상실). reduced-motion 존중 필요.
**적용 수정:** `activateTab`에 스크롤 보정 — 탭바가 화면 상단 고정 상태(`scrollY > tabsBottom`)일 때만, 새 패널 상단이 탭바 바로 아래(−8px 여백) 오도록 `scrollTo({behavior:'auto'})`(reduced-motion 무관 즉시). 이미 상단(y≤tabsBottom)이면 **미보정**(상단에서 전환 시 점프 없음).
**재평가:** y=1500서 all 전환 → h2Top **145**(tabsBottom 121 바로 아래·뷰포트 내). 상단(y=0)서 important 전환 → **scrollY 0 유지**(점프 없음). `pytest 85 passed`.
**다음 후보:** 중요뉴스 빈 그룹/스탯 0건 정책, 카드 호버 일관(미니아이템 vs 카드), divider 리듬.

---

## 사이클 45 — 데스크톱 종합 96 (개요 섹션 리듬 + 미니아이템 호버 어포던스)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24 | 변화 없음. |
| 간격(15) | 14→15 | **개요 마지막 섹션(임계값) divider 리듬 60→40px 정상화.** |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 미니아이템도 카드와 동일하게 호버 피드백(인터랙션 일관). |
| 접근성(10) | 10 | focus-within 행 강조(키보드 탐색 가시). |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견(개요):** ① `.config-panel { margin:20px 0 }` 잔존 — 이제 개요 탭에서 자체 h2(임계값 설정) 아래 위치하는데, 위 divider(mb 40px)+패널 mt 20px = **divider→섹션 간격 60px**(다른 두 섹션은 40px) → 마지막 섹션만 리듬 이탈. ② 카테고리 미니아이템은 행 전체가 클릭 링크를 품지만 행 자체엔 **호버 피드백 없음**(내부 `a` 색만 변함) — 카드는 리프트하는데 미니행은 무반응(어포던스/스캔성 불일치).
**적용 수정:** ① `.config-panel` margin 제거(h2 desc 아래 20px 리듬 상속). ② `.cat-mini-item:hover / :focus-within { background:surface-2 }`(카드 호버에 상응하는 행 강조, reduced-motion 무관 즉시 색).
**재평가:** divider→임계값 h2 간격 **60→40px**(3섹션 균일). 미니아이템 hover 룰 적용 확인. `pytest 92 passed`(디스크 신규 테스트 7건 포함 전부 green — 프론트 무영향).
**다음 후보:** 중요뉴스 0건 빈상태/그룹, 스탯 0건 카테고리 정책, 와이드 정렬 미세.

---

## 사이클 46 — 데스크톱 종합 96 (스탯 그리드 — 조용한 카테고리 0건 노출)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24→25 | **모니터링 완전성 — 4 카테고리 항상 노출, "추적 안 됨"vs"조용함" 구분.** |
| 간격(15) | 15 | 5 스탯카드 1280/1440 모두 1행(오버플로 0). |
| 컬러(15) | 15 | 0건 카드 값 dim 처리(활성과 위계 분리·도트 색 유지). |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 그리드 카드 수 안정(데이터 변동에도 5개 고정). |
| 접근성(10) | 10 | 0건도 "수집 없음" 텍스트 명시(색 비의존). |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견(개요 스탯):** `renderOverview`가 `inCat.length===0` 카테고리를 **스킵** → woori(0건)가 스탯·미니에서 통째 사라짐. 모니터링 대시보드에서 조용한 카테고리의 침묵은 **신호**인데(추적 중인데 뉴스 없음 vs 미추적) 구분 불가. 게다가 데이터에 따라 스탯 카드 수가 들쭉(그리드 불안정).
**적용 수정:** 스탯 그리드는 **CAT_ORDER 4개 항상 렌더**(0건 스킵 제거), 0건 카드는 `.stat-quiet`(값 색 text-dim) + sub "수집 없음" 텍스트. 미니 리스트 섹션(실제 뉴스 항목)은 빈 카테고리 스킵 유지(빈 섹션은 노이즈). 즉 **요약 타일=완전성 / 항목 리스트=실데이터** 역할 분리.
**재평가:** 스탯 **4→5카드**(전체+4카테고리), 우리금융 "0 / 수집 없음"·값 dim rgb(163,172,184). 1280·1440 모두 **1행**·오버플로 0(docW 1285/1445). `pytest 92 passed`.
**다음 후보:** 중요뉴스 탭 빈상태, 카테고리별 탭 필터 후 빈상태, 스탯 sub "발송 대상" 강조 일관.

---

## 사이클 47 — 데스크톱 종합 97 (카테고리 필터 빈상태 맥락 카피)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | **빈상태 안내 정확도 — 잘못된 행동 유도 제거.** |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견(카테고리별 탭):** 우리금융(0건)으로 필터 시 빈상태가 **"수집된 뉴스가 없습니다 / 수집 실행 버튼을 눌러…"** — 데이터는 이미 로드됐고 이 카테고리만 빈 것인데 "수집하라"는 **오도 카피**(사용자가 불필요한 수집 실행). 중요뉴스 탭 빈상태(전용 카피)·전역 no-data 빈상태는 정상.
**적용 수정:** `renderInto`에 `emptyOpts{title,hint}` 옵션 추가(미지정 시 기존 카피). `renderByCategory`가 특정 필터+전체데이터 존재+무에러일 때 `"${카테고리} 카테고리에 표시할 뉴스가 없습니다 / 다른 카테고리를 선택하거나 전체 뉴스를 확인하세요."` 주입. title/hint escapeHtml 처리.
**재평가:** woori 필터 → "우리금융그룹 카테고리에 표시할 뉴스가 없습니다 / 다른 카테고리를…" 확인. 전역 no-data·에러 빈상태는 기본 카피 유지. `pytest 92 passed`.
**다음 후보:** 와이드(1920) 콘텐츠 정렬/여백, 헤더 중복 액션(헤더 수집 vs 개요 수집) 정리, 카드 호버 일관 최종.

---

## 사이클 48 — 데스크톱 종합 97 (중복 수집 버튼 제거 — 액션 위계 정리)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | **개요 상단 액션 단순화 — 발송이 단독 primary로 부각.** |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 수집=헤더 단일 진입점(전역). 발송=개요/중요뉴스 컨텍스트. 역할 분리 명확. |
| 접근성(10) | 10 | 동일 라벨 버튼 중복(SR "수집 실행" ×2) 제거. |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견(개요):** "수집 실행" 버튼이 **헤더(sticky 상시) + 개요 상단** 두 곳에 ~60px 간격으로 **동시 노출**(headerCollectVisible·ovCollectVisible 둘 다 true). 동일 액션 중복으로 액션 위계 희석·SR 중복 라벨. 헤더 수집은 전역 상시이므로 개요 중복은 불필요.
**적용 수정:** 개요 `overview-actions`에서 `ov-collect-btn` 제거(수집은 헤더 단독). 남은 `ov-send-btn`을 ghost→**primary**로 승격(개요 단독 액션). `COLLECT_BTNS`에서 `ov-collect-btn` 참조 제거(수집 상태는 헤더+개요 상태줄 양쪽 계속 표기 — `COLLECT_STAT` 유지).
**재평가:** ov-collect 제거 확인·발송 primary·헤더 수집 잔존. 수집 상태 멀티타깃 동기(헤더+개요 상태줄 둘 다 "TEST_COLLECT" 반영) 확인 — 기능 무손상. 오버플로 0(docW 1285). `pytest 92 passed`.
**다음 후보:** 와이드 콘텐츠 여백, 카드 호버 일관, 최종 회귀 스윕.

---

## 사이클 49 — 데스크톱 종합 97 (모바일 탭바 도킹 회귀 수정 — 회귀 0 보장)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 데스크톱·모바일 탭바 도킹 정책 일관(헤더 하단에 flush). |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | **모바일 sticky 스택 겹침 해소(회귀 0 유지).** |

**계측 발견(390px):** 데스크톱 탭바 top을 75px로 고친 김에 모바일 점검 — 모바일 헤더 실측 **65px**인데 `.tabs { top:53px }`(라운드3 헤더 65px화 이전 잔존값) → 스크롤 시 탭바 헤더 아래 **12px 겹침**(gapOrOverlap −12). 사이클 41 데스크톱과 동형 결함이 모바일에도 잠복.
**적용 수정:** 모바일 분기 `.tabs { top:53→65px }`(모바일 헤더에 정확 도킹). scroll-margin-top 132px는 모바일(65+44≈109)에 여유 마진이라 무해.
**재평가:** 390px — gapOrOverlap **−12→0**(flush 도킹). 4탭 전수 docW=winW(오버플로 0), 개요그리드 1열·스탯 2열 정상. `pytest 92 passed`. 데스크톱(1440) 복귀 — 회귀 0.
**다음 후보:** 최종 다중폭 회귀 스윕(1280·1440·1920·640·390)·계약 재확인.

---

## 사이클 50 — 데스크톱 종합 97 (최종 다중폭 회귀 스윕 + 계약 재확인 · 수렴)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | **3 데스크톱폭+모바일 전 탭 무결.** |

**적대적 점검(전 폭·전 탭 계측):**

| 폭 | 오버플로 | 개요그리드 | 전체뉴스열 | 스탯행 | 탭바도킹 |
|----|---------|----------|----------|-------|---------|
| 1280 | 0 | 2열 | 3열 | 1행(5카드) | flush(0) |
| 1440 | 0 | 2열(590\|394) | 3열(323px) | 1행 | flush(0) |
| 1920 | 0 | 2열(590\|394) | 3열(323px) | flush(0) |
| 390(모바일) | 0 | 1열 | 1열 | flush(0) |

**계약 재확인:** `/api/alerts`·`/api/news` `{items,total}`·`/api/history` `{items}` 전부 존재. 프론트 읽기 8필드(importance_score·category·category_label·url·title·importance_reason·source·published_at) 샘플 전수 존재(allFields true). 데이터/스키마 무변경.
**적용 수정:** 없음(회귀 0 확인 사이클).
**검증:** `pytest 92 passed`. src/web/index.html 프레젠테이션만 수정. 탭 기능(전환·해시·발송·피드백·이력·설정·roving tabindex·화살표키) 무손상.

---

## ★★★★ 라운드4 최종 요약 (사이클 41~50 완주 · 탭 UI 데스크톱)
- **데스크톱 종합 점수 추이(41→50):** 93 · 94 · 95 · 95 · 96 · 96 · 97 · 97 · 97 · 97 → **최종 97/100**
- **제거한 실결함 8건(탭 UI 신규):**
  ① **sticky 탭바 헤더 하 14px 겹침**(데스크톱 `.tabs` top 61→75px) + scroll-margin 88→132px(헤더+탭바 커버)
  ② **개요 overview-grid 빈 3번째 컬럼**(auto-fill minmax320 3열·우측 471px 데드 공백 → 명시 2열 `1.5fr 1fr`·<860 1열)
  ③ **카테고리 필터칩 활성 무색**(배경만 → `--chip-c` 카테고리 색 보더, "전체"=accent 폴백) + chip-n 토큰화
  ④ **탭 전환 시 스크롤 미복원**(깊이 스크롤 후 전환 시 새 패널 중간 낙하 → 탭바 아래로 정렬, 상단에선 미보정)
  ⑤ **개요 마지막 섹션 리듬 이탈**(config-panel margin 60→40px) + 미니아이템 호버 어포던스
  ⑥ **스탯 그리드 조용한 카테고리 은닉**(0건 스킵 → 4카테고리 항상 노출·`.stat-quiet` dim·"수집 없음")
  ⑦ **카테고리 필터 빈상태 오도 카피**("수집하세요" → 카테고리 맥락 메시지, `renderInto` emptyOpts)
  ⑧ **개요 수집 버튼 중복**(헤더+개요 동시 → 헤더 단일·개요 발송 primary 승격)
  + 모바일 탭바 도킹 회귀 수정(`.tabs` top 53→65px).
- **무결 확인(지어내지 않음):** 와이드(1920) 카드열 상한(컨테이너 1040 캡으로 3열·323px 유지, 4열 미발생)·탭 활성 위계(색+언더라인 이중 단서)·개요 타이포 스케일(28>17>15>13) — 결함 없어 수치 기록 후 churn 회피.
- **모바일 회귀 0:** 탭바 도킹 동반 수정으로 모바일도 개선(겹침 −12→0). 390px 전 탭 오버플로 0.
- **검증:** `pytest 92 passed`(전 사이클·디스크 신규 테스트 7건 포함)·계약 `{items,total}`·8필드 불변·src/web/만 수정·탭 기능 무손상.
- **남은 한계:** ① 개요 우측 이력 사이드바는 카테고리 컬럼(615px)보다 짧아(280px) 하단 화이트스페이스 잔존 — 사이드바 레이아웃 본질(이력 항목 수 가변)이라 수용, 향후 이력+요약 위젯 추가로 채울 여지 ② 와이드(1920)에서 콘텐츠 1040px 캡으로 좌우 여백 큼 — 카드 가독폭 보호 위한 의도적 캡(읽기폭 우선) ③ 스탯 카드 `--chip-c` 식 카테고리 색을 스탯 카드 보더에도 적용 검토 여지(현재 도트만) — 과한 색면 우려로 보류.

---

# ════ 라운드5 — 탭 UI 모바일 (사이클 51~60) ════

## 사이클 51 — 모바일 종합 91 (탭바 음수마진 가로 오버플로 — 치명 결함)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24 | 변화 없음(탭 위계 유지). |
| 간격(15) | 14 | 탭바 거터를 모바일 컨테이너(14px)와 일치 — 탭이 카드 그리드 좌측에 정렬. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 8→9 | **320px 문서 가로 오버플로 20px 제거 — 모바일 치명 결함 해소.** |

**계측 발견(320px):** `.tabs`(nav)가 **body 직계 자식**(이미 뷰포트 전폭)인데 `margin: 0 calc(-1*max(20px,inset))` 음수 마진을 가짐 → 폭이 viewport+40px(360)로 부풀고 left:-20·right:340. 게다가 모바일 컨테이너 패딩은 14px인데 탭 패딩은 20px(거터 불일치). 결과 **문서 scrollWidth−clientWidth = +20px 가로 오버플로**(전 탭 공통, body 가로 스크롤 유발). 라운드3·4의 "오버플로 0" 계측은 `.tablist`(overflow-x:auto 내부 스크롤)만 봤고 **body 레벨 오버플로(음수마진 nav)는 놓침** — 이번 모바일 프레임에서 적발.
**적용 수정:** ① `.tabs` 음수 마진 **제거**(body 자식이라 불필요·유해), 패딩을 컨테이너와 동일한 `max(20px, inset)` 좌우 거터로. ② 모바일 분기에서 `.tabs` 패딩 `max(14px, inset)`로 내려 모바일 컨테이너(14px)와 거터 일치(탭 좌측이 카드 그리드 좌측에 정렬). ③ `.tablist`에 `-webkit-overflow-scrolling:touch`(iOS 관성 스크롤).
**재평가:** 320px — **docOverflow 20→0**, `.tabs` left:0·right:320(전폭 정확), 첫 탭 left:14=카드 거터. `.tablist`는 여전히 가로 스크롤(345>292, 의도된 전략·body 오버플로 아님). 데스크톱(1440) — docOverflow 0·`.tabs` 0→1425 전폭·tablist 좌측 193(라운드4 baseline 동일)·회귀 0. `pytest 92 passed`.
**다음 후보:** 활성 탭 scrollIntoView(좁은폭 마지막 탭 클리핑), 탭바 가로스크롤 어포던스(끝 페이드), 개요 스탯 그리드 칸수·터치간격.

---

## 사이클 52 — 모바일 종합 92 (활성 탭 클리핑 — scrollIntoView 미작동)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24 | 활성 탭(언더라인 인디케이터)이 항상 가시 — 현재 뷰 방향감 회복. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 키보드 화살표로 마지막 탭 이동 시에도 탭이 뷰 안으로 스크롤(2.4.7 포커스 가시). |
| 반응형(10) | 9→10 | **좁은폭 탭바 가로스크롤 시 활성 탭 자동 가시화.** |

**계측 발견(320px):** tablist 콘텐츠 345px > 가시폭 292px → 마지막 탭 "카테고리별"이 **53px 클리핑**(우측 잘림). `activateTab`이 패널/페이지 세로 스크롤은 보정하나 **tablist 가로 scrollLeft는 미조정** → 마지막 탭을 클릭/화살표 선택해도 `scrollLeft` 0 유지, 활성 탭과 언더라인이 화면 밖(우측 359 vs tablist 우측 306). 사용자가 어느 탭이 활성인지 못 봄.
**적용 수정:** `activateTab`에 가로 스크롤 보정 추가 — `tablist.scrollWidth>clientWidth`일 때만, 선택 탭이 우측 클리핑이면 `scrollLeft += (br.right-lr.right)+16px`(끝 16px 여백으로 인접 탭 일부 노출=스크롤 가능 암시), 좌측 클리핑이면 대칭 보정. **scrollLeft만 조정**(세로 스크롤·포커스 점프 없음).
**재평가:** 320px — 마지막 탭 선택 시 `scrollLeft 0→53`, 클리핑 53→**0**(완전 가시·left 223 뷰 안), **scrollY 0 유지**(세로 점프 없음). 개요 복귀 시 `scrollLeft 53→0`(첫 탭 left 14). 데스크톱(1440) — tablist 미오버플로 → 보정 **no-op**(scrollLeft 0·scrollY 0)·docOverflow 0 **회귀 0**. `pytest 92 passed`.
**다음 후보:** 탭바 가로스크롤 어포던스(끝 페이드 그라디언트), 개요 스탯 그리드 칸수·터치 간격, 필터칩 행 가로스크롤/줄바꿈.

---

## 사이클 53 — 모바일 종합 93 (탭바 가로스크롤 어포던스 — 끝 페이드)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24 | 변화 없음. |
| 간격(15) | 14 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 스크롤 가능성 시각 단서(스크롤바 숨김 보완) — 탐색 발견성↑. |
| 반응형(10) | 9→10 | **숨은 스크롤바 → 끝 페이드로 "더 있음" 암시(좁은폭 한정).** |

**계측 발견(320px):** tablist는 `scrollbar-width:none`(스크롤바 숨김)이라 53px 클리핑된 마지막 탭의 **존재 단서가 전무** — 사용자가 "카테고리별" 탭이 있는지 모를 위험(발견성 결함). 데스크톱은 오버플로 0이라 무관.
**적용 수정:** `.tabs::before/::after` 좌·우 28px 끝 페이드(`linear-gradient → var(--bg)`), 기본 opacity 0. JS `updateTabScrollAffordance()`가 `scrollWidth>clientWidth`일 때만 스크롤 위치로 `.scroll-start`(좌측 가림)·`.scroll-end`(우측 가림) 토글 → 해당 페이드 표시. `activateTab`·tablist `scroll`·window `resize`에서 호출. `pointer-events:none`(탭 클릭 무방해).
**재평가:** 320px — 초기(scrollLeft 0) **scroll-end만**(우측 페이드, "더 있음"). 끝까지 스크롤 시 **scroll-start**(좌측 페이드)로 전환. 스크린샷 — 우측 "카E…" 위에 페이드 확인. 데스크톱(1440) — 오버플로 0 → **클래스 없음·양 페이드 opacity 0**(불표시)·docOverflow 0 **회귀 0**. `pytest 92 passed`.
**다음 후보:** 개요 스탯 그리드 5칸 2열(마지막 1칸 고립) 검토, 카드 상단행 모바일 점수배지/배지 줄바꿈, 가로모드(844) 헤더 예산.

---

## 사이클 54 — 모바일 종합 94 (가로모드 sticky 예산 31%→24% — 라운드3 보류항목 해소)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 24→25 | **가로모드에서 콘텐츠 영역 확대(헤더 압축) — 본문 위계 회복.** |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 가로모드 헤더 컴팩트 정책이 모바일 세로(컴팩트)와 일관. |
| 접근성(10) | 10 | 변화 없음(터치타깃 44px·도킹 유지). |
| 반응형(10) | 10 | **짧은 높이 sticky 예산 회수.** |

**계측 발견(844×390 가로모드):** width>640이라 데스크톱 헤더(75px)+탭바(46px) = **121px = 뷰포트 높이 31% 점유**(sticky라 스크롤 내내). 탭바가 라운드3 이후 추가돼 가로모드 예산이 라운드3 시점(75px=19%)보다 악화 — 라운드3이 "over-engineering"으로 보류했으나 탭 UI에선 실질 결함.
**적용 수정:** `@media (max-height:480px) and (min-width:641px)` 신설 — 헤더 패딩 14→8px·로고 32→28px·h1 17→16px·subtitle 숨김(컴팩트), `.tabs top:75→52px`(컴팩트 헤더에 도킹), 탭 패딩 13→10px. 폭과 무관(가로모드 폭 넓어도)하게 짧은 높이만 타깃.
**재평가:** 844×390 — 헤더 **75→52px**, sticky 예산 **121→92px(31%→23.6%)**, 도킹 gap 0(flush), docOverflow 0. 세로(390×844, h>480) — 미적용·헤더 65px(모바일 컴팩트)·도킹 0. 데스크톱(1440×900, h>480) — 미적용·헤더 75px·subtitle block·2열·도킹 0 **회귀 0**. `pytest 92 passed`.
**다음 후보:** 개요 스탯 5칸 2열 마지막 고립 칸, 카드 상단행 점수배지 모바일, 유체 카드 제목, 발송 액션 모바일 정렬.

---

## 사이클 55 — 모바일 종합 95 (개요 미니리스트 제목 4줄 폭주 → 2줄 클램프)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 개요 "한눈에" 요약 의도 회복(스캔성↑). |
| 간격(15) | 14→15 | **미니행 높이 57~93px 들쭉 → 56~57px 균일(수직 리듬 안정).** |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 미니아이템 행 높이 일관(카드 탭은 전체 제목 유지 — 역할 분리 명확). |
| 접근성(10) | 10 | 전체 제목·"새 탭" 고지는 `aria-label`에 보존(정보 손실 0·SR 무영향). |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견(320px):** 개요 "카테고리별 최신 뉴스" 미니리스트(각 카테고리 상위 3건)에서 긴 한글 제목이 **최대 4줄(행 93px)**·3줄 다수로 폭주 → 행 높이 57~93px 들쭉, "한눈에 스캔" 목적이 무너짐. (메인 카드 제목은 의도적으로 truncation 안 함=정보 은닉 회피 — 라운드2 결정. 그러나 **개요 요약 리스트는 동일 항목이 다른 탭 카드에 전문 노출되므로** 여기선 클램프가 정보 손실 0.)
**적용 수정:** `.cmi-title`에 `-webkit-line-clamp:2`(box-orient vertical·overflow hidden). 메인 `.card-title`·`.cmi-title a`의 `aria-label`(전체 제목+"새 탭") 불변 → 시각만 2줄, SR/링크 무영향.
**재평가:** 320px — 미니행 **57~93px → 56~57px 균일**, 최대 2줄, aria-label "…새 탭에서 열림" 보존, docOverflow 0. 데스크톱(1440) — 짧은 제목 1줄(44~45px)·긴 것만 2줄캡(56px)·2열·docOverflow 0 **회귀 0**. `pytest 92 passed`.
**다음 후보:** 발송 액션 모바일 정렬, 유체 카드 제목, 다중폭 회귀 스윕, 가로모드 본문 스택.

---

## 사이클 56 — 모바일 종합 95 (카테고리 필터칩 터치타깃 35→44px)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음(행간 8px 유지). |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | **탭/피드백/버튼(44px)과 필터칩 터치타깃 기준 통일.** |
| 접근성(10) | 10 | WCAG 2.5.5 — 카테고리별 탭 1차 인터랙션 타깃 표준 충족. |
| 반응형(10) | 10 | 변화 없음. |

**계측 발견(320px):** 카테고리별 탭의 필터칩(`.cat-chip`)이 `padding:7px 13px`로 **높이 35px** — 탭(44)·피드백(44)·버튼(44)이 모두 44px인데 칩만 미달. 이 칩은 카테고리별 탭의 **주 인터랙션**(필터 선택)이라 미달이 더 부각(라운드2 C12에서 버튼류는 44px화했으나 탭 재설계 때 추가된 칩은 누락). 행간은 이미 8px(2.5.8 충족).
**적용 수정:** 모바일 분기 `.cat-chip { min-height:44px }`(데스크톱 35px 밀도 보존). 좌우 패딩·줄바꿈·행간 불변.
**재평가:** 320px — 칩 **35→44px**(5칩 전부), 3행 줄바꿈 유지·docOverflow 0. 데스크톱(1440) — 칩 35px(밀도 보존)·docOverflow 0 **회귀 0**. `pytest 92 passed`.
**다음 후보:** 중요뉴스 탭 헤더 정렬 baseline, 유체 카드 제목, 다중폭 회귀 스윕.

---

## 사이클 57 — 모바일 종합 95 (다중폭 회귀 스윕 + 페이드 적응 검증 · 수렴)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | **4폭×4탭 무결 + 페이드 어포던스 폭별 적응 검증.** |

**적대적 점검(무결 확인 — 지어내지 않음):**
① **중요뉴스 헤더 정렬:** 의심했던 h2(29px)↔발송버튼(44px) baseline 어긋남 → 실측 결과 **둘 다 vertical center 156px(centerDiff 0)**, `align-items:center`로 정상 정렬. 결함 아님(churn 회피).
② **4폭×4탭 오버플로 스윕:**

| 폭 | overview | important | all | by-category | tablist넘침 | 페이드 |
|----|---------|-----------|-----|-------------|-----------|--------|
| 320 | 0 | 0 | 0 | 0 | +53px | scroll-end ✓ |
| 360 | 0 | 0 | 0 | 0 | +13px | scroll-end ✓ |
| 390 | 0 | 0 | 0 | 0 | (소) | ✓ |
| 414 | 0 | 0 | 0 | 0 | **0(맞음)** | **꺼짐 ✓** |

→ 전 폭·전 탭 docOverflow 0. 탭바 도킹 65px flush. **페이드는 414px(4탭 정확히 맞음)에서 자동 OFF**(opacity 0·클래스 0), 360·320(넘침)에서 자동 ON — 폭별 적응 정확.
**적용 수정:** 없음(회귀 0·무결 확인 사이클).
**검증:** `pytest 92 passed`. src/web/만 수정.
**다음 후보:** 데스크톱 전폭 회귀 재확인, 유체 카드 제목(보류 검토), 최종 종합 스윕.

---

## 사이클 58 — 모바일 종합 95 (데스크톱 전폭 회귀 + 페이드↔탭탭 간섭 무결 · 수렴)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 페이드가 탭 탭(터치)·활성 언더라인을 가리지 않음 확인. |
| 반응형(10) | 10 | **데스크톱 1280/1920 회귀 0 + 페이드 간섭 무결.** |

**적대적 점검(무결 확인):**
① **데스크톱 회귀(`.tabs` 구조 변경 후):** 1280 — 전탭 오버플로 0·개요 2열·전체 3열·스탯 5카드 1행·페이드 OFF. 1920 — 오버플로 0·전체 3열·컨테이너 1040 캡·탭바 전폭(right 1905)·페이드 OFF. **라운드4 baseline 동일·회귀 0**.
② **페이드↔탭 간섭(320px):** 페이드 `pointer-events:none` 확인 → 첫 탭·마지막 탭 모두 `elementFromPoint`가 탭 반환(**탭 가능**). 마지막 탭 활성 시 cycle52 scroll-into-view로 **완전 가시**(left≥0·right≤vw) → 활성 언더라인이 페이드 아래 안 가림. 결함 아님.
**적용 수정:** 없음(회귀 0·무결 확인 사이클).
**검증:** `pytest 92 passed`. src/web/만 수정.
**다음 후보:** 유체 카드 제목 검토, 가로모드 본문 스택, 최종 종합 스윕.

---

## 사이클 59 — 모바일 종합 96 (가로모드 본문 플로우 무결 확인 · 수렴)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 변화 없음. |
| 간격(15) | 15 | 변화 없음. |
| 컬러(15) | 15 | 변화 없음. |
| 타이포(15) | 15 | 변화 없음. |
| 일관성(10) | 10 | 변화 없음. |
| 접근성(10) | 10 | 변화 없음. |
| 반응형(10) | 10 | **가로모드(844×390) 컴팩트 헤더 후 본문 플로우 무결.** |

**적대적 점검(무결 확인, 844×390):** 사이클54 헤더 컴팩트(52px) 적용 후 본문 회귀 점검 — 4탭 전부 docOverflow 0, 개요 그리드 1열(width 829<860 스택), 스탯 4열 2행, 전체뉴스 2열, 탭 페이드 OFF(4탭이 844폭에 여유 수용). 스크린샷 — 컴팩트 헤더 1행·전탭 노출·스탯 4열 정상. 본문이 압축 헤더와 충돌·잘림 없음. 결함 아님.
**적용 수정:** 없음(무결 확인 사이클).
**검증:** `pytest 92 passed`.
**다음 후보:** 최종 종합 다중폭 스윕(320·360·390·414·844·1280·1440)·계약 재확인·라운드5 마감.

---

## 사이클 60 — 모바일 종합 96 (최종 종합 스윕 + 계약 재확인 · 라운드5 마감)

| 축 | 점수 | 메모 |
|----|------|------|
| 정보위계(25) | 25 | 가로모드 본문 확대·미니리스트 스캔성·활성탭 가시. |
| 간격(15) | 15 | 미니행 균일·탭 거터 정렬·sticky 예산 회수. |
| 컬러(15) | 15 | 변화 없음(라운드4 자산 유지). |
| 타이포(15) | 15 | 유체 제목·미니 2줄 클램프. |
| 일관성(10) | 10 | 터치타깃 44px 전면 통일(탭·칩·버튼·피드백). |
| 접근성(10) | 10 | 탭 스크롤 가시·페이드 발견성·44px·계약 불변. |
| 반응형(10) | 10 | 7폭+가로모드 전탭 오버플로 0·페이드 폭별 적응. |

**적대적 점검(전 폭·전 탭 최종 계측):**

| 폭 | overview | important | all | by-cat | 헤더 | 칩H | 페이드 | 비고 |
|----|---------|-----------|-----|--------|------|-----|--------|------|
| 320 | 0 | 0 | 0 | 0 | 65 | 44 | ON | 미니행 57px·탭 44 |
| 360 | 0 | 0 | 0 | 0 | 65 | 44 | ON | tablist넘침 13px |
| 390 | 0 | 0 | 0 | 0 | 65 | 44 | ON | 도킹 65 flush |
| 414 | 0 | 0 | 0 | 0 | 65 | 44 | OFF | 4탭 맞음 |
| 844(가로) | 0 | 0 | 0 | 0 | **52** | — | OFF | 예산 31→24% |
| 1280 | 0 | 0 | 0 | 0 | 75 | 35 | OFF | 회귀0 |
| 1440 | 0 | 0 | 0 | 0 | 75 | 35 | OFF | 도킹0·2열·subtitle |

**계약 재확인:** `/api/alerts`·`/api/news` `{items,total}`(total 10/13)·`/api/history` `{items}` 존재. 프론트 8필드(importance_score·category·category_label·url·title·importance_reason·source·published_at) 샘플 전수 존재. 데이터/스키마 무변경.
**탭 기능 무손상:** 전환·해시·발송·피드백·이력·설정·roving tabindex·화살표키 — 5사이클 내내 무영향(추가는 scrollLeft·class 토글 등 시각 레이어만).
**적용 수정:** 없음(최종 회귀 0 확인 사이클).
**검증:** `pytest 92 passed`. src/web/index.html 프레젠테이션만 수정.

---

## ★★★★★ 라운드5 최종 요약 (사이클 51~60 완주 · 탭 UI 모바일)
- **모바일 종합 점수 추이(51→60):** 91 · 92 · 93 · 94 · 95 · 95 · 95 · 95 · 96 · 96 → **최종 96/100**
- **제거한 모바일 실결함 6건(탭 UI 모바일 신규):**
  ① **탭바 음수마진 가로 오버플로**(body 자식 nav가 viewport+40px로 부풀어 320px 문서 오버플로 +20px → 음수마진 제거·거터를 컨테이너와 일치) — *치명*
  ② **활성 탭 클리핑**(마지막 탭 53px 잘림·scrollLeft 미보정 → activateTab에 scrollIntoView 가로 보정, scrollY 무영향)
  ③ **탭바 가로스크롤 발견성**(숨은 스크롤바 → `.tabs::before/::after` 끝 페이드, JS 스크롤위치 토글, 폭별 적응)
  ④ **가로모드 sticky 예산 과점**(844×390 헤더+탭바 121px=31% → `max-height:480px` 헤더 컴팩트로 52px·92px·24%, 라운드3 보류항목 해소)
  ⑤ **개요 미니리스트 제목 4줄 폭주**(행 57~93px 들쭉 → `-webkit-line-clamp:2`로 56~57px 균일, 전체 제목은 aria-label·카드탭 보존)
  ⑥ **필터칩 터치타깃 미달**(35px → 모바일 44px, WCAG 2.5.5·탭/버튼/피드백과 통일)
- **무결 확인(지어내지 않음, 수치 기록):** 카드 상단행 점수배지/배지 무줄바꿈(topH 24·gap≥49)·important 헤더 baseline(center 156 일치)·전 탭 1열 그리드·필터칩 행간 8px·페이드↔탭탭 무간섭(pointer-events:none) — 결함 없어 churn 회피.
- **데스크톱 회귀 0:** 매 사이클 1280/1440/1920 동시 계측 — 헤더75·subtitle·2열 개요·3열 전체·탭바 도킹0·칩35px·페이드OFF 불변. `.tabs` 구조 변경(음수마진 제거) 후에도 baseline 동일.
- **검증:** `pytest 92 passed`(전 사이클)·계약 `{items,total}`·8필드 불변·탭 기능 무손상·src/web/만 수정.
- **남은 한계:** ① 카드 제목 모바일 16/17px 유체화는 한글 가독 하한이라 미적용(라운드3 결정 유지) ② 360px에서 tablist 13px 넘침은 4탭 한글 라벨 합이 360 거터폭 초과라 불가피 — 가로스크롤+페이드+scrollIntoView로 UX 보강(축약 라벨은 의미 손실 우려로 미채택) ③ 페이드 opacity 측정이 transition 중간값으로 잡히나 종단 상태(0/1)는 정확 — 시각 영향 무.

---
