# 뉴스 모니터링 MVP (우리금융그룹 수직 슬라이스) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 우리금융그룹 AI/데이터 뉴스를 토스인베스트에서 추출 → 키워드+LLM 중요도 평가 → SQLite 저장 → FastAPI 데모 대시보드에 표시하는 동작하는 수직 슬라이스를 만든다.

**Architecture:** `collect → filter → score → store → API → dashboard` 단방향 파이프라인. 단일 소스(우리금융, 토스 종목 A316140)로 전체 흐름을 먼저 검증한 뒤, 동일 인터페이스로 소스를 확장한다. 모든 데이터는 표준 `NewsItem` 계약을 따르고 SQLite에 영속화된다.

**Tech Stack:** Python 3.11+, pytest(TDD), Scrapling(JS 크롤링), feedparser(RSS, 확장용), anthropic SDK(중요도 평가), FastAPI + uvicorn(데모 대시보드), 표준 라이브러리 sqlite3, PyYAML(설정).

---

## File Structure

| 파일 | 책임 |
|------|------|
| `requirements.txt` | 의존성 |
| `config.yaml` | 임계값·키워드·소스 설정 (운영자 조정) |
| `src/models.py` | `NewsItem` dataclass + `Category` 상수 + id 생성 |
| `src/storage.py` | SQLite 스키마 init, upsert, 조회 |
| `src/collectors/base.py` | `Collector` 인터페이스 |
| `src/collectors/woori.py` | 우리금융 토스 수집기 (Scrapling) + 파싱 함수 |
| `src/filters/keyword.py` | 1단계 키워드 필터 |
| `src/filters/scorer.py` | 2단계 LLM 중요도 평가 |
| `src/filters/postprocess.py` | 3·4단계 임계값 + 중복제거 |
| `src/pipeline.py` | collect→filter→score→store 조립 |
| `src/api/app.py` | FastAPI 엔드포인트 + 정적 서빙 |
| `src/web/index.html` | 데모 대시보드 (바닐라 JS) |
| `tests/...` | 각 모듈 테스트 |
| `tests/fixtures/woori_news.html` | 토스 페이지 파싱 테스트용 고정 HTML |

각 파일은 단일 책임을 갖는다. 수집기는 정규화만, 필터는 판정만, 저장소는 영속화만 담당한다.

---

## Task 0: 프로젝트 스캐폴드

**Files:**
- Create: `requirements.txt`, `config.yaml`, `.gitignore`, `src/__init__.py`, `tests/__init__.py`

- [ ] **Step 1: git 저장소 초기화 (아직 git repo 아님)**

```bash
git init
```

- [ ] **Step 2: requirements.txt 작성**

```
scrapling>=0.2
feedparser>=6.0
anthropic>=0.40
fastapi>=0.110
uvicorn>=0.29
pyyaml>=6.0
pytest>=8.0
httpx>=0.27
```

- [ ] **Step 3: config.yaml 작성**

```yaml
thresholds:
  global_ai: 8.5
  global_finance_ai: 7.0
  domestic_finance_ai: 4.0
monthly_cap:
  global_finance_ai: 1
collect_interval_hours: 6
dedup_window_hours: 24
batch_window_minutes: 90
keyword_filters: [AI, 인공지능, LLM, 머신러닝, 데이터, 디지털, 마이데이터]
retry_max: 3
sources:
  woori:
    name: 우리금융지주
    toss_stock: A316140
    category: domestic_finance_ai
```

- [ ] **Step 4: .gitignore 작성**

```
__pycache__/
*.pyc
.venv/
data/*.db
.env
```

- [ ] **Step 5: 패키지 디렉토리 생성**

```bash
mkdir -p src/collectors src/filters src/api src/web tests/fixtures data
touch src/__init__.py src/collectors/__init__.py src/filters/__init__.py src/api/__init__.py tests/__init__.py
```

- [ ] **Step 6: 의존성 설치**

Run: `python -m venv .venv`
그다음 **TLS 가로채는 프록시 환경 대응**으로 trusted-host를 venv pip 설정에 영구 등록한 뒤 설치한다 (이 환경에서 pypi.org가 SSL EOF로 막히는 것을 확인함):
```
.venv\Scripts\pip config set --site global.trusted-host "pypi.org files.pythonhosted.org"
.venv\Scripts\pip install -r requirements.txt
```
Expected: 설치 성공. (Scrapling 최초 라이브 크롤링 시 `.venv\Scripts\scrapling install`로 브라우저 바이너리 설치 필요 — Task 10에서만, 단위 테스트엔 불필요. 브라우저 바이너리 CDN도 프록시에 막히면 라이브 크롤링은 보류하고 fixture 기반 테스트로 검증)

- [ ] **Step 7: Commit**

```bash
git add requirements.txt config.yaml .gitignore src tests
git commit -m "chore: scaffold news monitoring MVP project"
```

---

## Task 1: NewsItem 데이터 모델

**Files:**
- Create: `src/models.py`
- Test: `tests/test_models.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_models.py
from src.models import NewsItem, Category, make_id

def test_make_id_is_stable_and_normalized():
    a = make_id("https://example.com/news/1?utm_source=x")
    b = make_id("https://example.com/news/1")
    assert a == b                      # 쿼리스트링 무시
    assert len(a) == 16

def test_newsitem_roundtrip_dict():
    item = NewsItem(
        id=make_id("https://t.com/1"), category=Category.DOMESTIC_FINANCE_AI,
        title="우리금융 AI 도입", url="https://t.com/1", source="토스인베스트",
        published_at="2026-05-29T08:00:00+09:00", collected_at="2026-05-29T09:00:00+09:00",
        summary_raw="요약",
    )
    d = item.to_dict()
    assert d["category"] == "domestic_finance_ai"
    assert NewsItem.from_dict(d).title == "우리금융 AI 도입"

def test_defaults_for_unscored_item():
    item = NewsItem(id="x", category=Category.GLOBAL_AI, title="t", url="u",
                    source="s", published_at="", collected_at="", summary_raw="")
    assert item.keyword_passed is False
    assert item.importance_score is None
    assert item.send_recommended is False
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `.venv\Scripts\pytest tests/test_models.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'src.models'`

- [ ] **Step 3: 최소 구현**

```python
# src/models.py
import hashlib
from dataclasses import dataclass, asdict, field
from urllib.parse import urlsplit, urlunsplit


class Category:
    GLOBAL_AI = "global_ai"
    GLOBAL_FINANCE_AI = "global_finance_ai"
    DOMESTIC_FINANCE_AI = "domestic_finance_ai"
    ALL = (GLOBAL_AI, GLOBAL_FINANCE_AI, DOMESTIC_FINANCE_AI)


LABELS = {
    Category.GLOBAL_AI: "글로벌 AI",
    Category.GLOBAL_FINANCE_AI: "글로벌 금융 AI",
    Category.DOMESTIC_FINANCE_AI: "국내 금융 AI",
}


def make_id(url: str) -> str:
    parts = urlsplit(url.strip().lower())
    normalized = urlunsplit((parts.scheme, parts.netloc, parts.path, "", ""))
    return hashlib.sha256(normalized.encode()).hexdigest()[:16]


@dataclass
class NewsItem:
    id: str
    category: str
    title: str
    url: str
    source: str
    published_at: str
    collected_at: str
    summary_raw: str
    keyword_passed: bool = False
    importance_score: float | None = None
    importance_reason: str = ""
    send_recommended: bool = False
    dedup_group: str | None = None
    sent: bool = False
    sent_at: str | None = None

    def to_dict(self) -> dict:
        return asdict(self)

    @classmethod
    def from_dict(cls, d: dict) -> "NewsItem":
        return cls(**{k: d.get(k) for k in cls.__dataclass_fields__})
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `.venv\Scripts\pytest tests/test_models.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add src/models.py tests/test_models.py
git commit -m "feat: add NewsItem model with stable id generation"
```

---

## Task 2: Storage (SQLite)

**Files:**
- Create: `src/storage.py`
- Test: `tests/test_storage.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_storage.py
from src.models import NewsItem, Category, make_id
from src.storage import Storage

def _item(url, score=None):
    return NewsItem(id=make_id(url), category=Category.DOMESTIC_FINANCE_AI,
                    title="t", url=url, source="토스", published_at="2026-05-29T00:00:00+09:00",
                    collected_at="2026-05-29T01:00:00+09:00", summary_raw="s",
                    importance_score=score)

def test_upsert_is_idempotent(tmp_path):
    st = Storage(tmp_path / "t.db")
    st.upsert([_item("https://t.com/1")])
    st.upsert([_item("https://t.com/1")])      # 같은 url → 같은 id
    assert len(st.query()) == 1

def test_query_filters_by_min_score(tmp_path):
    st = Storage(tmp_path / "t.db")
    st.upsert([_item("https://t.com/1", score=2.0), _item("https://t.com/2", score=9.0)])
    high = st.query(min_score=5.0)
    assert len(high) == 1 and high[0].url == "https://t.com/2"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `.venv\Scripts\pytest tests/test_storage.py -v`
Expected: FAIL — `No module named 'src.storage'`

- [ ] **Step 3: 최소 구현**

```python
# src/storage.py
import sqlite3
from pathlib import Path
from src.models import NewsItem

_FIELDS = list(NewsItem.__dataclass_fields__)

_SCHEMA = """
CREATE TABLE IF NOT EXISTS news_items (
  id TEXT PRIMARY KEY, category TEXT, title TEXT, url TEXT, source TEXT,
  published_at TEXT, collected_at TEXT, summary_raw TEXT,
  keyword_passed INTEGER DEFAULT 0, importance_score REAL, importance_reason TEXT,
  send_recommended INTEGER DEFAULT 0, dedup_group TEXT,
  sent INTEGER DEFAULT 0, sent_at TEXT
);
CREATE TABLE IF NOT EXISTS send_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT, news_id TEXT, category TEXT, title TEXT,
  channel TEXT, sent_at TEXT, batch_id TEXT
);
CREATE TABLE IF NOT EXISTS feedback (
  id INTEGER PRIMARY KEY AUTOINCREMENT, news_id TEXT, kind TEXT, note TEXT, created_at TEXT
);
"""


class Storage:
    def __init__(self, db_path):
        Path(db_path).parent.mkdir(parents=True, exist_ok=True)
        self.conn = sqlite3.connect(str(db_path))
        self.conn.row_factory = sqlite3.Row
        self.conn.executescript(_SCHEMA)
        self.conn.commit()

    def upsert(self, items: list[NewsItem]) -> None:
        rows = [tuple(self._encode(getattr(it, f)) for f in _FIELDS) for it in items]
        placeholders = ",".join("?" * len(_FIELDS))
        cols = ",".join(_FIELDS)
        self.conn.executemany(
            f"INSERT OR REPLACE INTO news_items ({cols}) VALUES ({placeholders})", rows)
        self.conn.commit()

    def query(self, category=None, min_score=None) -> list[NewsItem]:
        sql, args = "SELECT * FROM news_items WHERE 1=1", []
        if category:
            sql += " AND category=?"; args.append(category)
        if min_score is not None:
            sql += " AND importance_score >= ?"; args.append(min_score)
        sql += " ORDER BY importance_score DESC NULLS LAST, collected_at DESC"
        return [self._row_to_item(r) for r in self.conn.execute(sql, args)]

    @staticmethod
    def _encode(v):
        return int(v) if isinstance(v, bool) else v

    @staticmethod
    def _row_to_item(r: sqlite3.Row) -> NewsItem:
        d = dict(r)
        for b in ("keyword_passed", "send_recommended", "sent"):
            d[b] = bool(d[b])
        return NewsItem.from_dict(d)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `.venv\Scripts\pytest tests/test_storage.py -v`
Expected: PASS (2 passed). 만약 `NULLS LAST` 구문 에러 시 SQLite 버전이 낮은 것이므로 `ORDER BY importance_score DESC, collected_at DESC`로 교체.

- [ ] **Step 5: Commit**

```bash
git add src/storage.py tests/test_storage.py
git commit -m "feat: add SQLite storage with idempotent upsert"
```

---

## Task 3: 우리금융 수집기 (Scrapling)

수집기를 `fetch`(네트워크)와 `parse`(순수 함수)로 분리하여 parse만 고정 HTML로 테스트한다. 네트워크는 단위 테스트하지 않는다.

**Files:**
- Create: `src/collectors/base.py`, `src/collectors/woori.py`, `tests/fixtures/woori_news.html`
- Test: `tests/test_woori_collector.py`

- [ ] **Step 1: 고정 HTML fixture 작성**

```html
<!-- tests/fixtures/woori_news.html -->
<html><body>
<ul>
  <li class="news-item">
    <a href="https://www.tossinvest.com/news/111">우리금융, 생성형 AI 플랫폼 전 계열사 확대</a>
    <time datetime="2026-05-29T08:30:00+09:00">2026.05.29</time>
    <span class="source">한국경제</span>
    <p class="desc">우리금융지주가 생성형 AI 기반 업무 플랫폼을...</p>
  </li>
  <li class="news-item">
    <a href="https://www.tossinvest.com/news/112">우리은행 신규 점포 오픈</a>
    <time datetime="2026-05-28T10:00:00+09:00">2026.05.28</time>
    <span class="source">머니투데이</span>
    <p class="desc">우리은행이 강남에 신규 점포를...</p>
  </li>
</ul>
</body></html>
```

- [ ] **Step 2: 실패하는 테스트 작성**

```python
# tests/test_woori_collector.py
from pathlib import Path
from src.collectors.woori import parse_woori_html
from src.models import Category

def test_parse_woori_html_normalizes_items():
    html = Path("tests/fixtures/woori_news.html").read_text(encoding="utf-8")
    items = parse_woori_html(html, source_name="토스인베스트")
    assert len(items) == 2
    first = items[0]
    assert first.category == Category.DOMESTIC_FINANCE_AI
    assert first.title == "우리금융, 생성형 AI 플랫폼 전 계열사 확대"
    assert first.url == "https://www.tossinvest.com/news/111"
    assert first.source == "한국경제"
    assert first.published_at == "2026-05-29T08:30:00+09:00"
    assert first.id == items[0].id and len(first.id) == 16
    assert first.summary_raw.startswith("우리금융지주가 생성형 AI")

def test_parse_handles_empty_html():
    assert parse_woori_html("<html></html>", source_name="토스") == []
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `.venv\Scripts\pytest tests/test_woori_collector.py -v`
Expected: FAIL — `No module named 'src.collectors.woori'`

- [ ] **Step 4: base 인터페이스 구현**

```python
# src/collectors/base.py
from src.models import NewsItem

class Collector:
    category: str
    def collect(self) -> list[NewsItem]:
        raise NotImplementedError
```

- [ ] **Step 5: woori 수집기 구현 (parse는 lxml로 fixture 테스트 가능)**

```python
# src/collectors/woori.py
from datetime import datetime, timezone
from lxml import html as lxml_html
from src.collectors.base import Collector
from src.models import NewsItem, Category, make_id


def _now_iso() -> str:
    return datetime.now(timezone.utc).astimezone().isoformat(timespec="seconds")


def parse_woori_html(html: str, source_name: str) -> list[NewsItem]:
    """순수 파싱 함수 — 네트워크 의존 없음. 적응형 셀렉터 전략을 모사한다."""
    tree = lxml_html.fromstring(html)
    items: list[NewsItem] = []
    for node in tree.cssselect("li.news-item"):
        a = node.cssselect("a")
        if not a:
            continue
        url = a[0].get("href", "").strip()
        title = (a[0].text_content() or "").strip()
        if not url or not title:
            continue
        time_nodes = node.cssselect("time")
        published = time_nodes[0].get("datetime", "").strip() if time_nodes else ""
        src_nodes = node.cssselect(".source")
        src = src_nodes[0].text_content().strip() if src_nodes else source_name
        desc_nodes = node.cssselect(".desc")
        desc = (desc_nodes[0].text_content().strip() if desc_nodes else "")[:1000]
        items.append(NewsItem(
            id=make_id(url), category=Category.DOMESTIC_FINANCE_AI, title=title,
            url=url, source=src, published_at=published, collected_at=_now_iso(),
            summary_raw=desc,
        ))
    return items


class WooriCollector(Collector):
    category = Category.DOMESTIC_FINANCE_AI
    URL = "https://www.tossinvest.com/stocks/A316140/news"

    def __init__(self, source_name: str = "토스인베스트"):
        self.source_name = source_name

    def collect(self) -> list[NewsItem]:
        # JS 렌더링 페이지 — Scrapling StealthyFetcher로 DOM 확보 후 parse 재사용.
        # 내부 JSON API가 확인되면 httpx 직접 호출로 교체 (더 안정적).
        from scrapling.fetchers import StealthyFetcher
        page = StealthyFetcher.fetch(self.URL, headless=True, network_idle=True)
        return parse_woori_html(page.html_content, self.source_name)
```

> 참고: `parse_woori_html`의 셀렉터(`li.news-item`, `time`, `.source`, `.desc`)는 fixture 기준이다. 실제 토스 DOM 확인 후 셀렉터를 조정하되, parse 함수 시그니처와 반환 계약(NewsItem 리스트)은 유지한다. `page.html_content` 속성명은 설치된 Scrapling 버전에 맞춰 확인한다(`.html_content` 또는 `.body`).

- [ ] **Step 6: 테스트 통과 확인**

Run: `.venv\Scripts\pytest tests/test_woori_collector.py -v`
Expected: PASS (2 passed). `lxml`가 없으면 `cssselect`와 함께 `.venv\Scripts\pip install lxml cssselect` 후 requirements.txt에 `lxml`, `cssselect` 추가.

- [ ] **Step 7: requirements.txt에 lxml 추가 + Commit**

```bash
git add src/collectors tests/test_woori_collector.py tests/fixtures/woori_news.html requirements.txt
git commit -m "feat: add Woori collector with testable HTML parser"
```

---

## Task 4: 키워드 필터

**Files:**
- Create: `src/filters/keyword.py`
- Test: `tests/test_keyword_filter.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_keyword_filter.py
from src.filters.keyword import keyword_filter
from src.models import NewsItem, Category, make_id

def _item(title, summary=""):
    return NewsItem(id=make_id(title), category=Category.DOMESTIC_FINANCE_AI, title=title,
                    url="u/"+title, source="s", published_at="", collected_at="", summary_raw=summary)

def test_keeps_items_with_keyword():
    items = [_item("우리금융 AI 도입"), _item("우리은행 점포 오픈")]
    kept = keyword_filter(items, ["AI", "인공지능"])
    assert len(kept) == 1 and kept[0].keyword_passed is True

def test_matches_in_summary_too():
    kept = keyword_filter([_item("우리금융 신규 발표", summary="마이데이터 기반 서비스")], ["마이데이터"])
    assert len(kept) == 1
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `.venv\Scripts\pytest tests/test_keyword_filter.py -v`
Expected: FAIL — `No module named 'src.filters.keyword'`

- [ ] **Step 3: 최소 구현**

```python
# src/filters/keyword.py
from src.models import NewsItem

def keyword_filter(items: list[NewsItem], keywords: list[str]) -> list[NewsItem]:
    """제목 또는 요약에 키워드가 하나라도 있으면 통과(keyword_passed=True)시키고 반환."""
    kept = []
    lowered = [k.lower() for k in keywords]
    for it in items:
        haystack = f"{it.title} {it.summary_raw}".lower()
        if any(k in haystack for k in lowered):
            it.keyword_passed = True
            kept.append(it)
    return kept
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `.venv\Scripts\pytest tests/test_keyword_filter.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add src/filters/keyword.py tests/test_keyword_filter.py
git commit -m "feat: add keyword pre-screening filter"
```

---

## Task 5: LLM 중요도 평가

응답 파싱(`parse_score_response`)을 순수 함수로 분리하고, 점수 채움 로직(`score`)은 주입된 호출 함수(fake)로 테스트한다. 실제 Anthropic 호출은 단위 테스트하지 않는다.

**Files:**
- Create: `src/filters/scorer.py`
- Test: `tests/test_scorer.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_scorer.py
from src.filters.scorer import parse_score_response, score
from src.models import NewsItem, Category, make_id

def _item():
    return NewsItem(id=make_id("u1"), category=Category.DOMESTIC_FINANCE_AI, title="우리금융 AI 플랫폼",
                    url="u1", source="s", published_at="", collected_at="", summary_raw="생성형 AI 도입")

def test_parse_score_response_extracts_fields():
    raw = '여기 결과입니다: {"score": 6.5, "reason": "전 계열사 확대로 의미 있음", "send": true}'
    parsed = parse_score_response(raw)
    assert parsed == {"score": 6.5, "reason": "전 계열사 확대로 의미 있음", "send": True}

def test_parse_score_response_handles_garbage():
    assert parse_score_response("모델이 JSON을 안 줌") is None

def test_score_fills_fields_using_injected_caller():
    def fake_caller(item):
        return '{"score": 7.2, "reason": "근거", "send": true}'
    out = score([_item()], caller=fake_caller)
    assert out[0].importance_score == 7.2
    assert out[0].importance_reason == "근거"
    assert out[0].send_recommended is True

def test_score_on_failure_leaves_item_unscored():
    def bad_caller(item):
        return "잘못된 응답"
    out = score([_item()], caller=bad_caller)
    assert out[0].importance_score is None
    assert out[0].send_recommended is False
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `.venv\Scripts\pytest tests/test_scorer.py -v`
Expected: FAIL — `No module named 'src.filters.scorer'`

- [ ] **Step 3: 최소 구현**

```python
# src/filters/scorer.py
import json
import re
from src.models import NewsItem, Category

_PROMPTS = {
    Category.GLOBAL_AI: "AI 산업 판도를 바꿀 수준인지 0~10으로 평가. 신규 파운데이션 모델, 수십억 달러 M&A, 규제 판도 변화, 패러다임 전환 연구는 8.5+. 제품 업데이트·마케팅·루머는 3 이하.",
    Category.GLOBAL_FINANCE_AI: "금융 AI 실용화 관점에서 의미 있는지 0~10으로 평가. 실제 도입·운영 사례, 신규 AI 금융 서비스, 공식 AI 전략 발표는 7+. 단순 언급은 4 이하.",
    Category.DOMESTIC_FINANCE_AI: "국내 금융지주의 AI·데이터 활동으로서 가치 있는지 0~10으로 평가(누락 방지 우선, 넓게). 신규 서비스, AI 조직·채용·파트너십, 투자·예산 발표, 데이터 거버넌스/마이데이터는 4+. AI 단어만 있는 홍보성은 2 이하.",
}


def build_prompt(item: NewsItem) -> str:
    criteria = _PROMPTS.get(item.category, _PROMPTS[Category.DOMESTIC_FINANCE_AI])
    return (
        f"{criteria}\n\n"
        f"제목: {item.title}\n출처: {item.source}\n발행: {item.published_at}\n본문요약: {item.summary_raw}\n\n"
        '오직 JSON만 출력: {"score": 0.0~10.0, "reason": "1~2문장", "send": true/false}'
    )


def parse_score_response(raw: str) -> dict | None:
    m = re.search(r"\{.*\}", raw, re.DOTALL)
    if not m:
        return None
    try:
        d = json.loads(m.group(0))
        return {"score": float(d["score"]), "reason": str(d["reason"]), "send": bool(d["send"])}
    except (json.JSONDecodeError, KeyError, ValueError, TypeError):
        return None


def default_caller(item: NewsItem) -> str:
    """실제 Anthropic 호출. 단위 테스트에서는 fake로 대체된다."""
    import anthropic
    client = anthropic.Anthropic()
    resp = client.messages.create(
        model="claude-haiku-4-5-20251001", max_tokens=300,
        messages=[{"role": "user", "content": build_prompt(item)}],
    )
    return resp.content[0].text


def score(items: list[NewsItem], caller=default_caller) -> list[NewsItem]:
    for it in items:
        parsed = parse_score_response(caller(it))
        if parsed is None:
            it.importance_score = None
            it.send_recommended = False
            continue
        it.importance_score = parsed["score"]
        it.importance_reason = parsed["reason"]
        it.send_recommended = parsed["send"]
    return items
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `.venv\Scripts\pytest tests/test_scorer.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: Commit**

```bash
git add src/filters/scorer.py tests/test_scorer.py
git commit -m "feat: add LLM importance scorer with injectable caller"
```

---

## Task 6: 임계값 + 중복제거

**Files:**
- Create: `src/filters/postprocess.py`
- Test: `tests/test_postprocess.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_postprocess.py
from src.filters.postprocess import apply_threshold, dedup
from src.models import NewsItem, Category, make_id

def _item(url, score, title="우리금융 AI 발표", send=True):
    return NewsItem(id=make_id(url), category=Category.DOMESTIC_FINANCE_AI, title=title, url=url,
                    source="s", published_at="2026-05-29T00:00:00+09:00", collected_at="",
                    summary_raw="", importance_score=score, send_recommended=send)

def test_apply_threshold_clears_below_cutoff():
    items = [_item("u1", 3.0), _item("u2", 5.0)]
    out = apply_threshold(items, thresholds={Category.DOMESTIC_FINANCE_AI: 4.0})
    assert out[0].send_recommended is False     # 3.0 < 4.0
    assert out[1].send_recommended is True       # 5.0 >= 4.0

def test_dedup_keeps_one_per_group():
    items = [_item("u1", 5.0, title="우리금융 생성형 AI 도입"),
             _item("u2", 6.0, title="우리금융 생성형 AI 도입 발표")]
    out = dedup(items)
    groups = {it.dedup_group for it in out}
    assert len(groups) == 1                       # 유사 제목 → 같은 그룹
    recommended = [it for it in out if it.send_recommended]
    assert len(recommended) == 1                  # 그룹 내 1건만 유지
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `.venv\Scripts\pytest tests/test_postprocess.py -v`
Expected: FAIL — `No module named 'src.filters.postprocess'`

- [ ] **Step 3: 최소 구현**

```python
# src/filters/postprocess.py
import re
from src.models import NewsItem


def apply_threshold(items: list[NewsItem], thresholds: dict) -> list[NewsItem]:
    """카테고리별 임계값 미만이면 send_recommended를 False로 끈다."""
    for it in items:
        cutoff = thresholds.get(it.category, 5.0)
        if it.importance_score is None or it.importance_score < cutoff:
            it.send_recommended = False
    return items


def _norm_tokens(title: str) -> set[str]:
    cleaned = re.sub(r"[^0-9a-z가-힣 ]", " ", title.lower())
    return {t for t in cleaned.split() if len(t) > 1}


def _similar(a: set[str], b: set[str], threshold: float = 0.6) -> bool:
    if not a or not b:
        return False
    jaccard = len(a & b) / len(a | b)
    return jaccard >= threshold


def dedup(items: list[NewsItem]) -> list[NewsItem]:
    """유사 제목을 같은 dedup_group으로 묶고, 그룹 내 점수 최고 1건만 send_recommended 유지."""
    groups: list[list[NewsItem]] = []
    for it in items:
        tokens = _norm_tokens(it.title)
        placed = False
        for g in groups:
            if _similar(tokens, _norm_tokens(g[0].title)):
                g.append(it); placed = True; break
        if not placed:
            groups.append([it])
    for gi, g in enumerate(groups):
        gid = f"g{gi}"
        winner = max(g, key=lambda x: (x.importance_score or -1))
        for it in g:
            it.dedup_group = gid
            if it is not winner:
                it.send_recommended = False
    return items
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `.venv\Scripts\pytest tests/test_postprocess.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: Commit**

```bash
git add src/filters/postprocess.py tests/test_postprocess.py
git commit -m "feat: add threshold gating and title-similarity dedup"
```

---

## Task 7: 파이프라인 조립

**Files:**
- Create: `src/pipeline.py`
- Test: `tests/test_pipeline.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_pipeline.py
from src.pipeline import run_pipeline
from src.storage import Storage
from src.models import NewsItem, Category, make_id

def _raw(url, title):
    return NewsItem(id=make_id(url), category=Category.DOMESTIC_FINANCE_AI, title=title, url=url,
                    source="토스", published_at="2026-05-29T00:00:00+09:00", collected_at="2026-05-29T01:00:00+09:00",
                    summary_raw="생성형 AI 도입")

def test_run_pipeline_collects_filters_scores_stores(tmp_path):
    st = Storage(tmp_path / "p.db")
    collected = [_raw("https://t/1", "우리금융 AI 플랫폼 확대"), _raw("https://t/2", "우리은행 점포 오픈")]
    fake_collect = lambda: collected
    fake_caller = lambda item: '{"score": 6.0, "reason": "의미 있음", "send": true}'
    config = {"keyword_filters": ["AI"], "thresholds": {Category.DOMESTIC_FINANCE_AI: 4.0}}

    result = run_pipeline(collectors=[fake_collect], storage=st, config=config, caller=fake_caller)

    stored = st.query()
    assert len(stored) == 1                         # "점포 오픈"은 키워드 미통과로 제외
    assert stored[0].importance_score == 6.0
    assert stored[0].send_recommended is True
    assert result["collected"] == 2 and result["stored"] == 1
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `.venv\Scripts\pytest tests/test_pipeline.py -v`
Expected: FAIL — `No module named 'src.pipeline'`

- [ ] **Step 3: 최소 구현**

```python
# src/pipeline.py
from src.filters.keyword import keyword_filter
from src.filters.scorer import score, default_caller
from src.filters.postprocess import apply_threshold, dedup


def run_pipeline(collectors, storage, config, caller=default_caller) -> dict:
    """collect → keyword → score → threshold → dedup → store. collectors는 collect()형 콜러블 리스트."""
    collected = []
    for collect in collectors:
        try:
            collected.extend(collect())
        except Exception as e:
            print(f"수집 실패: {e}")
    passed = keyword_filter(collected, config.get("keyword_filters", []))
    scored = score(passed, caller=caller)
    gated = apply_threshold(scored, config.get("thresholds", {}))
    final = dedup(gated)
    storage.upsert(final)
    return {"collected": len(collected), "kept": len(passed), "stored": len(final)}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `.venv\Scripts\pytest tests/test_pipeline.py -v`
Expected: PASS (1 passed)

- [ ] **Step 5: Commit**

```bash
git add src/pipeline.py tests/test_pipeline.py
git commit -m "feat: wire collect-filter-score-store pipeline"
```

---

## Task 8: FastAPI 엔드포인트

**Files:**
- Create: `src/api/app.py`
- Test: `tests/test_api.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# tests/test_api.py
from fastapi.testclient import TestClient
from src.api.app import create_app
from src.storage import Storage
from src.models import NewsItem, Category, make_id

def _seed(st):
    st.upsert([NewsItem(id=make_id("u1"), category=Category.DOMESTIC_FINANCE_AI, title="우리금융 AI",
        url="u1", source="토스", published_at="2026-05-29T00:00:00+09:00", collected_at="",
        summary_raw="요약", importance_score=6.0, importance_reason="근거", send_recommended=True)])

def test_news_endpoint_returns_wrapped_list(tmp_path):
    st = Storage(tmp_path / "a.db"); _seed(st)
    client = TestClient(create_app(storage=st, config={}, run_collect=lambda: {"stored": 0}))
    r = client.get("/api/news")
    assert r.status_code == 200
    body = r.json()
    assert "items" in body and "total" in body          # 래핑 계약
    assert body["items"][0]["importance_reason"] == "근거"

def test_alerts_endpoint_only_recommended(tmp_path):
    st = Storage(tmp_path / "b.db"); _seed(st)
    client = TestClient(create_app(storage=st, config={}, run_collect=lambda: {"stored": 0}))
    body = client.get("/api/alerts").json()
    assert all(it["send_recommended"] for it in body["items"])

def test_collect_endpoint_triggers_pipeline(tmp_path):
    st = Storage(tmp_path / "c.db")
    client = TestClient(create_app(storage=st, config={}, run_collect=lambda: {"stored": 3}))
    assert client.post("/api/collect").json()["stored"] == 3
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `.venv\Scripts\pytest tests/test_api.py -v`
Expected: FAIL — `No module named 'src.api.app'`

- [ ] **Step 3: 최소 구현**

```python
# src/api/app.py
from pathlib import Path
from fastapi import FastAPI
from fastapi.responses import FileResponse
from src.models import LABELS

_WEB = Path(__file__).resolve().parent.parent / "web"


def _wrap(items):
    rows = [it.to_dict() for it in items]
    for r in rows:
        r["category_label"] = LABELS.get(r["category"], r["category"])
    return {"items": rows, "total": len(rows)}


def create_app(storage, config, run_collect):
    app = FastAPI(title="News Monitoring Demo")

    @app.get("/api/news")
    def news(category: str | None = None, min_score: float | None = None):
        return _wrap(storage.query(category=category, min_score=min_score))

    @app.get("/api/alerts")
    def alerts():
        return _wrap([it for it in storage.query() if it.send_recommended])

    @app.get("/api/config")
    def get_config():
        return config

    @app.post("/api/collect")
    def collect():
        return run_collect()

    @app.get("/")
    def index():
        return FileResponse(_WEB / "index.html")

    return app
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `.venv\Scripts\pytest tests/test_api.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Commit**

```bash
git add src/api/app.py tests/test_api.py
git commit -m "feat: add FastAPI endpoints with wrapped list responses"
```

---

## Task 9: 데모 대시보드 (정적)

대시보드는 수동 검증한다(자동 테스트 대상 아님). 빈 데이터에서도 배열 보장으로 깨지지 않게 한다.

**Files:**
- Create: `src/web/index.html`

- [ ] **Step 1: 대시보드 작성**

```html
<!-- src/web/index.html -->
<!doctype html>
<html lang="ko"><head><meta charset="utf-8">
<title>뉴스 모니터링 데모</title>
<style>
  body{font-family:system-ui,sans-serif;max-width:880px;margin:24px auto;padding:0 16px;color:#1a1a1a}
  h1{font-size:20px} .row{display:flex;gap:8px;align-items:center;margin:12px 0}
  button{padding:8px 14px;border:0;border-radius:8px;background:#2962ff;color:#fff;cursor:pointer}
  .card{border:1px solid #e3e3e3;border-radius:12px;padding:14px;margin:10px 0}
  .label{display:inline-block;font-size:12px;background:#eef2ff;color:#2962ff;padding:2px 8px;border-radius:999px}
  .score{float:right;font-weight:700}
  .reason{color:#555;font-size:14px;margin:6px 0}
  .meta{color:#999;font-size:12px} a{color:#2962ff;text-decoration:none}
  .empty{color:#999;text-align:center;padding:32px}
</style></head>
<body>
  <h1>📰 뉴스 모니터링 데모</h1>
  <div class="row">
    <button onclick="collect()">수집 실행</button>
    <span id="status" class="meta"></span>
  </div>
  <h2>발송 대상 알림</h2>
  <div id="alerts"></div>
  <h2>전체 수집 뉴스</h2>
  <div id="news"></div>
<script>
async function load(){
  const a = await (await fetch('/api/alerts')).json();
  const n = await (await fetch('/api/news')).json();
  render('alerts', a.items); render('news', n.items);
}
function render(id, items){
  const el = document.getElementById(id);
  if(!items || items.length===0){ el.innerHTML='<div class="empty">표시할 뉴스가 없습니다.</div>'; return; }
  el.innerHTML = items.map(it=>`
    <div class="card">
      <span class="score">${it.importance_score ?? '-'}</span>
      <span class="label">${it.category_label}</span>
      <h3><a href="${it.url}" target="_blank">${it.title}</a></h3>
      <div class="reason">${it.importance_reason || ''}</div>
      <div class="meta">${it.source} · ${it.published_at}</div>
    </div>`).join('');
}
async function collect(){
  document.getElementById('status').textContent='수집 중...';
  const r = await (await fetch('/api/collect',{method:'POST'})).json();
  document.getElementById('status').textContent='수집 완료: '+JSON.stringify(r);
  load();
}
load();
</script>
</body></html>
```

- [ ] **Step 2: Commit**

```bash
git add src/web/index.html
git commit -m "feat: add demo dashboard page"
```

---

## Task 10: 엔드투엔드 실행 + 데모 검증

**Files:**
- Create: `run.py`

- [ ] **Step 1: 실행 진입점 작성**

```python
# run.py
import yaml
from pathlib import Path
import uvicorn
from src.storage import Storage
from src.collectors.woori import WooriCollector
from src.pipeline import run_pipeline
from src.api.app import create_app

CONFIG = yaml.safe_load(Path("config.yaml").read_text(encoding="utf-8"))
STORAGE = Storage("data/news.db")

def do_collect():
    woori = WooriCollector(source_name="토스인베스트")
    return run_pipeline(collectors=[woori.collect], storage=STORAGE, config=CONFIG)

app = create_app(storage=STORAGE, config=CONFIG, run_collect=do_collect)

if __name__ == "__main__":
    uvicorn.run(app, host="127.0.0.1", port=8000)
```

- [ ] **Step 2: 전체 테스트 통과 확인**

Run: `.venv\Scripts\pytest -v`
Expected: 모든 테스트 PASS

- [ ] **Step 3: 서버 실행 후 대시보드 수동 검증**

Run: `.venv\Scripts\python run.py`
브라우저에서 `http://127.0.0.1:8000` 접속 → 빈 상태 정상 렌더 확인 → "수집 실행" 클릭.

검증 항목:
- 우리금융 토스 페이지에서 뉴스가 수집되어 카드로 표시되는가
- 각 카드에 카테고리 레이블·점수·판단 근거·출처·발행일시·원문 링크가 모두 있는가
- 발송 대상(임계값 통과)만 "발송 대상 알림"에 표시되는가

> Scrapling 첫 실행에서 브라우저 바이너리 오류 시: `.venv\Scripts\scrapling install` 실행. 실제 토스 DOM이 fixture와 다르면 `parse_woori_html`의 셀렉터를 실제 구조에 맞게 조정(반환 계약은 유지).

- [ ] **Step 4: Commit**

```bash
git add run.py
git commit -m "feat: add entrypoint and verify end-to-end demo"
```

---

## Task 11: 하네스 확장 파일 생성 (source-researcher / threshold-tuner)

스펙 6절의 하네스 메타 파일을 생성한다. 마크다운 정의이므로 TDD 대상이 아니며, 구조 검증만 수행한다.

**Files:**
- Create: `.claude/agents/source-researcher.md`, `.claude/agents/threshold-tuner.md`
- Create: `.claude/skills/news-source-research/SKILL.md`, `.claude/skills/news-threshold-tuning/SKILL.md`
- Modify: `.claude/skills/news-monitoring-orchestrator/SKILL.md`, `.claude/skills/news-system-design/SKILL.md`, `CLAUDE.md`

- [ ] **Step 1: source-researcher 에이전트 정의 작성**

`.claude/agents/source-researcher.md` — frontmatter(name: source-researcher, description: 소스 실제 검증·선정 + 트리거 키워드 "소스 조사/소스 결정"). 본문: 역할(후보 RSS/API/페이지를 feedparser/Scrapling으로 fetch→파싱 성공·발행빈도·최신성·카테고리 적합도·중복률 측정→채택/탈락 목록), 입출력(`_workspace/00b_source_research.md`), `general-purpose` 타입 명시, robots.txt 검토 원칙.

- [ ] **Step 2: threshold-tuner 에이전트 정의 작성**

`.claude/agents/threshold-tuner.md` — frontmatter(name: threshold-tuner, description: FP/FN 피드백 분석→임계값/프롬프트 제안만 + 트리거 "임계값 튜닝/피드백 반영"). 본문: 역할(feedback+send_history 분석→제안만, 자동반영 금지, 운영자 승인 후 config 패치), 오버피팅 방지(원리 일반화), 출력(`_workspace/06_tuning_proposal.md`), `general-purpose` 타입.

- [ ] **Step 3: 두 스킬 SKILL.md 작성**

`news-source-research/SKILL.md`(소스 검증 절차·측정 지표·점수화 기준, description "pushy"), `news-threshold-tuning/SKILL.md`(피드백 분석법·제안 형식·오버피팅 방지). 둘 다 description에 후속 키워드 포함.

- [ ] **Step 4: 오케스트레이터 3모드 라우팅 반영**

`news-monitoring-orchestrator/SKILL.md` 수정: Phase 0 직후 요청 분류(리서치/빌드/튜닝) 추가, 에이전트 구성표에 source-researcher·threshold-tuner(서브 단독 실행) 추가, 데이터 흐름에 `00b_source_research.md`·`06_tuning_proposal.md` 반영, description에 "소스 조사/임계값 튜닝/피드백 반영" 키워드 추가.

- [ ] **Step 5: 빌드 모드의 소스 입력 연결**

`news-system-design/SKILL.md`에 "빌드 모드 시작 시 `_workspace/00b_source_research.md`가 있으면 읽어 소스 목록을 설계에 반영" 1줄 추가.

- [ ] **Step 6: CLAUDE.md 변경 이력 갱신**

`CLAUDE.md` 변경 이력 테이블에 행 추가: `| 2026-05-29 | source-researcher·threshold-tuner 추가, 오케스트레이터 3모드 라우팅, Scrapling 크롤링 확정 | agents/, skills/, orchestrator | 소스 결정·학습 루프 갭 보완 |`

- [ ] **Step 7: 구조 검증 + Commit**

Run: 신규 4개 파일의 frontmatter(name/description) 존재 확인, 오케스트레이터에 3모드·신규 에이전트 명시 확인, CLAUDE.md 이력 갱신 확인.
```bash
git add .claude CLAUDE.md
git commit -m "feat: extend harness with source-researcher and threshold-tuner agents"
```

---

## Self-Review

**1. Spec coverage (스펙 대비):**
- AI×금융 도메인(3카테고리) → Task 1 Category, Task 5 카테고리별 프롬프트 ✓
- 우리금융 소스 추출(Scrapling, A316140) → Task 3 ✓
- 소스 평가: 뉴스 중요도 평가 → Task 5·6 ✓ / 소스 선정(source-researcher) → Task 11 ✓
- 데모 페이지 게시 → Task 8·9·10 ✓
- 표준 NewsItem 계약 → Task 1, 전 모듈 준수, API 래핑 Task 8 ✓
- 하네스 확장(2 에이전트+오케스트레이터 3모드) → Task 11 ✓
- threshold-tuner는 정의만 생성(운영 실행은 MVP 이후) — 의도된 스코프 ✓

**2. Placeholder scan:** 코드 스텝은 모두 실제 코드 포함. Task 11은 마크다운 정의 생성으로 본문 요지를 구체 지시(파일 경로·frontmatter·필수 섹션 명시)했고 코드 스텝이 아니므로 플레이스홀더 아님. ✓

**3. Type consistency:** `NewsItem` 필드명이 models→storage→collector→filter→api 전체에서 일치. `run_pipeline(collectors, storage, config, caller)`, `score(items, caller)`, `apply_threshold(items, thresholds)`, `dedup(items)`, `create_app(storage, config, run_collect)`, `_wrap`의 `{items,total}` 래핑이 Task 8 테스트와 일치. `parse_woori_html(html, source_name)` 시그니처가 Task 3 테스트와 일치. ✓

> 알려진 미세 위험: 실제 토스 DOM과 fixture 셀렉터 차이(Task 3·10에서 조정 지시), Scrapling `.html_content` 속성명 버전 차이(Task 3 주석), SQLite `NULLS LAST` 미지원 구버전(Task 2 대안 제시). 모두 실행 단계 대응 지시 포함.
