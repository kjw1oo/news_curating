# 런타임 의존성 (로컬 파이프라인·서버 + Vercel 배포 공용).
# 토스 수집이 내부 JSON API(httpx)로 바뀌어 scrapling/playwright/curl_cffi는 더 이상 불필요.
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.0
pyyaml>=6.0
httpx>=0.27
feedparser>=6.0
lxml>=5.0
cssselect>=1.2
anthropic>=0.40
# 클라우드 DB(Turso) 연결은 src/turso_http.py가 httpx(위 의존성)로 처리 — 별도 드라이버 불필요.
